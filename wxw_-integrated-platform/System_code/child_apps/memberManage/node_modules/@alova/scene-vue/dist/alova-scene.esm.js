/**
  * @alova/scene-vue 1.6.2 (https://alova.js.org)
  * Copyright 2024 JOU-amjs. All Rights Reserved
  * Licensed under MIT (https://alova.js.org/blob/main/LICENSE)
  */

import { Method, getMethodKey, useWatcher, useFetcher, setCache, invalidateCache, queryCache, updateState, matchSnapshotMethod, useRequest } from 'alova';
import { ref, computed, watch, onMounted, onUnmounted } from 'vue';

// 以下为减少编译代码量而添加的统一处理函数或变量
const PromiseCls = Promise, undefinedValue = undefined, nullValue = null, trueValue = true, falseValue = false, ObjectCls = Object, NumberCls = Number, StringCls = String, BooleanCls = Boolean, RegExpCls = RegExp, symbolToStringTag = Symbol.toStringTag, STR_VALUE_OF = 'valueOf', DEFAUT_QUEUE_NAME = 'default', BEHAVIOR_SILENT = 'silent', BEHAVIOR_QUEUE = 'queue', BEHAVIOR_STATIC = 'static', 
// 是否为服务端渲染，为了兼容浏览器以及非web客户端环境（如小程序），需要再判断一下process
isSSR = typeof window === 'undefined' && typeof process !== 'undefined';

const promiseResolve = (value) => PromiseCls.resolve(value), promiseReject = (value) => PromiseCls.reject(value), promiseThen = (promise, onFulfilled, onrejected) => promise.then(onFulfilled, onrejected), promiseCatch = (promise, onrejected) => promise.catch(onrejected), promiseFinally = (promise, onfinally) => promise.finally(onfinally), forEach = (ary, fn) => ary.forEach(fn), pushItem = (ary, ...item) => ary.push(...item), filterItem = (ary, fn) => ary.filter(fn), map = (ary, fn) => ary.map(fn), includes = (ary, target) => ary.includes(target), len = (data) => data.length, isArray$1 = (target) => Array.isArray(target), shift = (ary) => ary.shift(), splice = (ary, start, deleteCount = 0, ...items) => ary.splice(start, deleteCount, ...items), getContextOptions = (alovaInstance) => alovaInstance.options, getConfig = (methodInstance) => methodInstance.config, getOptions = (methodInstance) => getContextOptions(getContext(methodInstance)), getContext = (methodInstance) => methodInstance.context, getMethodInternalKey = (methodInstance) => methodInstance.__key__, objectKeys = (obj) => ObjectCls.keys(obj), objectValues = (obj) => ObjectCls.values(obj), setTimeoutFn = (fn, delay = 0) => setTimeout(fn, delay), clearTimeoutFn = (timeoutId) => clearTimeout(timeoutId), regexpTest = (reg, str) => reg.test(str);
/**
 * 创建同步多次调用只在异步执行一次的执行器
 */
const createSyncOnceRunner = (delay = 0) => {
    let timer = undefinedValue;
    /**
     * 执行多次调用此函数将异步执行一次
     */
    return (fn) => {
        if (timer) {
            clearTimeoutFn(timer);
        }
        timer = setTimeoutFn(fn, delay);
    };
};
/**
 * 创建uuid简易版
 * @returns uuid
 */
const uuid = () => {
    const timestamp = new Date().getTime();
    return Math.floor(Math.random() * timestamp).toString(36);
};
/**
 * 兼容函数
 */
const noop$1 = () => { };
/**
 * 兼容函数，返回自身
 * @param value 任意值
 * @returns 自身
 */
const __self = (value) => value;
/**
 * 兼容函数，抛出参数
 * @param error 错误
 */
const __throw = (error) => {
    throw error;
};
// 判断是否为某个类的实例
const instanceOf = (arg, cls) => arg instanceof cls;
/**
 * 构建格式化的错误消息
 * @param prefix 错误前缀
 * @param msg 错误消息
 * @returns 格式化的错误消息
 */
const buildErrorMsg = (prefix, msg) => `[alova/${prefix}]${msg}`;
/**
 * 自定义断言函数，表达式为false时抛出错误
 * @param expression 判断表达式，true或false
 * @param msg 断言消息
 */
const createAssert = (prefix) => {
    return (expression, msg) => {
        if (!expression) {
            throw newInstance(Error, buildErrorMsg(prefix, msg));
        }
    };
};
const valueObject = (value, writable = falseValue) => ({
    value,
    writable
});
/**
 * 定义obj属性
 * @param o 对象
 * @param attrs 值对象
 */
const defineProperty = (o, key, value, isDescriptor = falseValue) => {
    ObjectCls.defineProperty(o, key, isDescriptor ? value : valueObject(value, falseValue));
};
/**
 * 批量执行时间回调函数，并将args作为参数传入
 * @param handlers 事件回调数组
 * @param args 函数参数
 */
const runArgsHandler = (handlers, ...args) => {
    let ret = undefinedValue;
    forEach(handlers, handler => {
        const retInner = handler(...args);
        ret = retInner !== undefinedValue ? retInner : ret;
    });
    return ret;
};
/**
 * typof冗余函数
 * @param arg 任意参数
 * @returns 参数类型
 */
const typeOf = (arg) => typeof arg;
/**
 * 判断参数是否为函数
 * @param fn 任意参数
 * @returns 该参数是否为函数
 */
const isFn = (arg) => typeOf(arg) === 'function';
/**
 * 判断参数是否为字符串
 * @param arg 任意参数
 * @returns 该参数是否为字符串
 */
const isString = (arg) => typeOf(arg) === 'string';
/**
 * 判断参数是否为对象
 * @param arg 任意参数
 * @returns 该参数是否为对象
 */
const isObject = (arg) => arg !== nullValue && typeOf(arg) === 'object';
/**
 * 判断是否为纯对象或自定义类的对象
 * @param arg 任意参数
 * @returns 该参数是否为纯对象或自定义类的对象
 */
const isPlainOrCustomObject = (arg) => ObjectCls.prototype.toString.call(arg) === '[object Object]';
const walkObject = (target, callback, preorder = trueValue, key, parent) => {
    const callCallback = () => {
        if (parent && key) {
            target = callback(target, key, parent);
            if (target !== parent[key]) {
                parent[key] = target;
            }
        }
    };
    // 前序遍历
    preorder && callCallback();
    if (isObject(target)) {
        for (const i in target) {
            if (!instanceOf(target, StringCls)) {
                walkObject(target[i], callback, preorder, i, target);
            }
        }
    }
    // 后序遍历
    !preorder && callCallback();
    return target;
};
/**
 * 创建类实例
 * @param cls 构造函数
 * @param args 构造函数参数
 * @returns 类实例
 */
const newInstance = (cls, ...args) => new cls(...args);
/**
 * 统一配置
 * @param 数据
 * @returns 统一的配置
 */
const sloughConfig = (config, args = []) => isFn(config) ? config(...args) : config;
const getTime = (date) => (date ? date.getTime() : Date.now());
/**
 * 判断参数是否为数字
 * @param arg 任意参数
 * @returns 该参数是否为数字
 */
const isNumber = (arg) => typeof arg === 'number' && !isNaN(arg);
/** 三种缓存模式 */
// 只在内存中缓存，默认是此选项
const MEMORY = 'memory', 
// 缓存会持久化，但当内存中没有缓存时，持久化缓存只会作为响应数据的占位符，且还会发送请求更新缓存
STORAGE_PLACEHOLDER = 'placeholder', 
// 缓存会持久化，且每次刷新会读取持久化缓存到内存中，这意味着内存一直会有缓存
STORAGE_RESTORE = 'restore';
/**
 * 获取缓存的配置参数，固定返回{ e: number, m: number, s: boolean, t: string }格式的对象
 * e为expire缩写，表示缓存失效时间点（时间戳），单位为毫秒
 * m为mode缩写，存储模式
 * s为storage缩写，是否存储到本地
 * t为tag缩写，持久化存储标签
 * @param localCache 本地缓存参数
 * @returns 统一的缓存参数对象
 */
const getLocalCacheConfigParam = (methodInstance, localCache) => {
    const _localCache = localCache !== undefinedValue ? localCache : methodInstance ? getConfig(methodInstance).localCache : undefinedValue;
    const getCacheExpireTs = (_localCache) => isNumber(_localCache) ? getTime() + _localCache : getTime(_localCache);
    let cacheMode = MEMORY;
    let expire = 0;
    let storage = falseValue;
    let tag = undefinedValue;
    if (!isFn(_localCache)) {
        if (isNumber(_localCache) || instanceOf(_localCache, Date)) {
            expire = getCacheExpireTs(_localCache);
        }
        else {
            const { mode = MEMORY, expire: configExpire = 0, tag: configTag } = _localCache || {};
            cacheMode = mode;
            expire = getCacheExpireTs(configExpire);
            storage = [STORAGE_PLACEHOLDER, STORAGE_RESTORE].includes(mode);
            tag = configTag ? configTag.toString() : undefinedValue;
        }
    }
    return {
        e: expire,
        m: cacheMode,
        s: storage,
        t: tag
    };
};
/**
 * 根据避让策略和重试次数计算重试延迟时间
 * @param backoff 避让参数
 * @param retryTimes 重试次数
 * @returns 重试延迟时间
 */
const delayWithBackoff = (backoff, retryTimes) => {
    let { delay, multiplier = 1, startQuiver, endQuiver } = backoff;
    let retryDelayFinally = (delay || 0) * Math.pow(multiplier, retryTimes - 1);
    // 如果startQuiver或endQuiver有值，则需要增加指定范围的随机抖动值
    if (startQuiver || endQuiver) {
        startQuiver = startQuiver || 0;
        endQuiver = endQuiver || 1;
        retryDelayFinally +=
            retryDelayFinally * startQuiver + Math.random() * retryDelayFinally * (endQuiver - startQuiver);
        retryDelayFinally = Math.floor(retryDelayFinally); // 取整数延迟
    }
    return retryDelayFinally;
};
/**
 * 获取请求方法对象
 * @param methodHandler 请求方法句柄
 * @param args 方法调用参数
 * @returns 请求方法对象
 */
const getHandlerMethod = (methodHandler, args = []) => {
    const methodInstance = isFn(methodHandler) ? methodHandler(...args) : methodHandler;
    createAssert('scene')(instanceOf(methodInstance, Method), 'hook handler must be a method instance or a function that returns method instance');
    return methodInstance;
};
function useCallback(onCallbackChange = noop$1) {
    let callbacks = [];
    const setCallback = (fn) => {
        if (!callbacks.includes(fn)) {
            callbacks.push(fn);
            onCallbackChange(callbacks);
        }
        // 返回取消注册函数
        return () => {
            callbacks = filterItem(callbacks, e => e !== fn);
            onCallbackChange(callbacks);
        };
    };
    const triggerCallback = (...args) => {
        if (callbacks.length > 0) {
            return forEach(callbacks, fn => fn(...args));
        }
    };
    const removeAllCallback = () => {
        callbacks = [];
        onCallbackChange(callbacks);
    };
    return [setCallback, triggerCallback, removeAllCallback];
}
function usePromise() {
    let _resolve;
    let _reject;
    const promise = new Promise((resolve, reject) => {
        _resolve = resolve;
        _reject = reject;
    });
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
    return { promise, resolve: _resolve, reject: _reject };
}

/**
 * 创建状态
 * @param data 创建状态的数据
 * @returns {FrameworkState}
 */
const $ = ref;

/**
 * 创建计算属性
 * @param getter 计算属性回调
 * @returns {FrameworkState}
 */
const $$ = (getter, _) => computed(getter, _);

/**
 * 脱水普通状态、计算属性或alova导出的状态，返回状态原始值
 * @param state 状态
 * @returns 状态原始值，即状态对应的数据
 */
const _$ = state => state.value;

/**
 * 状态导出
 * @param state 状态
 * @returns 状态原始值
 */
const _exp$ = state => state;

/**
 * 批量导出状态
 * @param state 状态
 * @returns 状态原始值
 */
const _expBatch$ = (...states) => map(states, s => _exp$(s));

/**
 * 更新状态值
 * @param state 更新的状态
 * @param newData 新状态值
 */
const upd$ = (state, newData) => (state.value = isFn(newData) ? newData(state.value) : newData);

/**
 * 监听状态触发回调
 * @param states 监听状态
 * @param {Function} cb 回调函数
 */
const watch$ = (states, cb) => {
  watch(states, cb, {
    deep: trueValue
  });
};

/**
 * 组件挂载执行
 * @param {Function} cb 回调函数
 */
const onMounted$ = cb => {
  onMounted(cb);
};

/**
 * 组件卸载执行
 * @param {Function} cb 回调函数
 */
const onUnmounted$ = cb => {
  onUnmounted(cb);
};

/**
 * 使用标识，一般作为标识
 * 在react中每次渲染都会调用hook，如果使用基础数据每次将会获得初始值
 * 兼容react
 * @param initialValue 初始值
 */
const useFlag$ = initialValue => ({ current: initialValue });

/**
 * 由于在react下，use hook返回的loading、data等状态为普遍值，将会受闭包影响
 * 此函数作为兼容react而存在
 * @param requestState 请求hook状态
 */
const useRequestRefState$ = __self;

/**
 * 由于在react下，如果每次传入子组件的callback引用变化会导致子组件重新渲染，而引起性能问题
 * 此函数作为兼容react而存在
 * @param callback
 * @returns
 */
const useMemorizedCallback$ = __self;

var createSnapshotMethodsManager = (handler) => {
    let methodSnapshots = {};
    return {
        snapshots: () => methodSnapshots,
        save(methodInstance, force = falseValue) {
            const key = getMethodKey(methodInstance);
            // 因为无法定位缓存中total数据的位置
            // 因此这边冗余维护这个字段
            if (!methodSnapshots[key] || force) {
                methodSnapshots[key] = {
                    entity: methodInstance
                };
            }
        },
        get: (entityOrPage) => methodSnapshots[getMethodKey(instanceOf(entityOrPage, Method) ? entityOrPage : handler(entityOrPage))],
        remove(key) {
            if (key) {
                delete methodSnapshots[key];
            }
            else {
                methodSnapshots = {};
            }
        }
    };
};

const paginationAssert = createAssert('usePagination');
const indexAssert = (index, rawData) =>
  paginationAssert(isNumber(index) && index < len(rawData), 'index must be a number that less than list length');

function usePagination_unified (
  handler,
  {
    preloadPreviousPage = trueValue,
    preloadNextPage = trueValue,
    total: totalGetter = res => res.total,
    data: dataGetter = res => res.data,
    append = falseValue,
    initialPage = 1,
    initialPageSize = 10,
    watchingStates = [],
    initialData,
    immediate = trueValue,
    middleware = noop$1,
    force = noop$1,
    ...others
  },
  $,
  $$,
  upd$,
  _$,
  _exp$,
  _expBatch$,
  watch$,
  useFlag$,
  useRequestRefState$,
  useMemorizedCallback$
) {
  const handerRef = useFlag$();
  handerRef.current = handler;
  const isReset = useFlag$(falseValue), // 用于控制是否重置
    // 重置期间请求的次数，为了防止重置时重复请求，使用此参数限制请求
    requestCountInReseting = useFlag$(0),
    page = $(initialPage, trueValue),
    pageSize = $(initialPageSize, trueValue),
    data = $(initialData ? dataGetter(initialData) || [] : [], trueValue),
    // 保存当前hook所使用到的所有method实例快照
    {
      snapshots: methodSnapshots,
      get: getSnapshotMethods,
      save: saveSnapshot,
      remove: removeSnapshot
    } = useFlag$(createSnapshotMethodsManager(page => handerRef.current(page, _$(pageSize)))).current,
    listDataGetter = rawData => dataGetter(rawData) || rawData,
    getHandlerMethod = (refreshPage = _$(page)) => {
      const pageSizeVal = _$(pageSize);
      const handlerMethod = handler(refreshPage, pageSizeVal);

      // 定义统一的额外名称，方便管理
      saveSnapshot(handlerMethod);
      return handlerMethod;
    };
  // 监听状态变化时，重置page为1
  watch$(watchingStates, () => {
    upd$(page, initialPage);
    requestCountInReseting.current = 0;
    isReset.current = trueValue;
  });

  // 兼容react，将需要代理的函数存放在此
  // 这样可以在代理函数中调用到最新的操作函数，避免react闭包陷阱
  const delegationActions = useFlag$({}),
    createDelegationAction =
      actionName =>
      (...args) =>
        delegationActions.current[actionName](...args),
    states = useWatcher(getHandlerMethod, [...watchingStates, _exp$(page), _exp$(pageSize)], {
      immediate,
      initialData,
      middleware(ctx, next) {
        middleware(
          {
            ...ctx,
            delegatingActions: {
              refresh: createDelegationAction('refresh'),
              insert: createDelegationAction('insert'),
              remove: createDelegationAction('remove'),
              replace: createDelegationAction('replace'),
              reload: createDelegationAction('reload'),
              getState: stateKey => {
                const states = {
                  page,
                  pageSize,
                  data,
                  pageCount,
                  total,
                  isLastPage
                };
                return _$(states[stateKey]);
              }
            }
          },
          promiseResolve
        );

        // 监听值改变时将会重置为第一页，此时会触发两次请求，在这边过滤掉一次请求
        let requestPromise = promiseResolve();
        if (!isReset.current) {
          requestPromise = next();
        } else if (requestCountInReseting.current === 0) {
          requestCountInReseting.current++;
          requestPromise = next();
        }
        return requestPromise;
      },
      force: (...args) => args[1] || force(...args),
      abortLast: false,
      ...others
    }),
    { send } = states,
    // 计算data、total、isLastPage参数
    total = $(initialData ? totalGetter(initialData) : undefinedValue, trueValue),
    pageCount = $$(
      () => {
        const totalVal = _$(total);
        return totalVal !== undefinedValue ? Math.ceil(totalVal / _$(pageSize)) : undefinedValue;
      },
      _expBatch$(pageSize, total),
      trueValue
    ),
    requestDataRef = useRequestRefState$(states.data);

  // 判断是否可预加载数据
  const canPreload = (
    rawData = _$(requestDataRef),
    preloadPage,
    fetchMethod,
    isNextPage = falseValue,
    forceRequest
  ) => {
    const { e: expireMilliseconds } = getLocalCacheConfigParam(fetchMethod);
    // 如果缓存时间小于等于当前时间，表示没有设置缓存，此时不再预拉取数据
    // 或者已经有缓存了也不预拉取
    if (expireMilliseconds <= getTime()) {
      return falseValue;
    }
    if (forceRequest) {
      return trueValue;
    }
    if (queryCache(fetchMethod)) {
      return falseValue;
    }

    const pageCountVal = _$(pageCount);
    const exceedPageCount = pageCountVal
      ? preloadPage > pageCountVal
      : isNextPage // 如果是判断预加载下一页数据且没有pageCount的情况下，通过最后一页数据量是否达到pageSize来判断
      ? len(listDataGetter(rawData)) < _$(pageSize)
      : falseValue;
    return preloadPage > 0 && !exceedPageCount;
  };

  // 预加载下一页数据
  const fetchNextPage = (rawData, force = falseValue) => {
    const nextPage = _$(page) + 1;
    const fetchMethod = getHandlerMethod(nextPage);
    if (preloadNextPage && canPreload(rawData, nextPage, fetchMethod, trueValue, force)) {
      promiseCatch(fetch(fetchMethod, force), noop$1);
    }
  };
  // 预加载上一页数据
  const fetchPreviousPage = rawData => {
    const prevPage = _$(page) - 1;
    const fetchMethod = getHandlerMethod(prevPage);
    if (preloadPreviousPage && canPreload(rawData, prevPage, fetchMethod)) {
      promiseCatch(fetch(fetchMethod), noop$1);
    }
  };
  // 如果返回的数据小于pageSize了，则认定为最后一页了
  const isLastPage = $$(
    () => {
      const dataRaw = _$(requestDataRef);
      if (!dataRaw) {
        return trueValue;
      }
      const statesDataVal = listDataGetter(dataRaw);
      const pageVal = _$(page);
      const pageCountVal = _$(pageCount);
      const dataLen = isArray$1(statesDataVal) ? len(statesDataVal) : 0;
      return pageCountVal ? pageVal >= pageCountVal : dataLen < _$(pageSize);
    },
    _expBatch$(page, pageCount, states.data, pageSize),
    trueValue
  );

  // 更新当前页缓存
  const updateCurrentPageCache = () => {
    const snapshotItem = getSnapshotMethods(_$(page));
    snapshotItem &&
      setCache(snapshotItem.entity, rawData => {
        // 当关闭缓存时，rawData为undefined
        if (rawData) {
          const cachedListData = listDataGetter(rawData) || [];
          splice(cachedListData, 0, len(cachedListData), ..._$(data));
          return rawData;
        }
      });
  };

  // 初始化fetcher
  const fetchStates = useFetcher({
    force: forceFetch => forceFetch
  });
  const { fetching, fetch, abort: abortFetch, onSuccess: onFetchSuccess } = fetchStates;
  const fetchingRef = useRequestRefState$(fetching);
  onFetchSuccess(({ method, data: rawData }) => {
    // 处理当fetch还没响应时就翻页到fetch对应的页码时，需要手动更新列表数据
    const snapshotItem = getSnapshotMethods(_$(page));
    if (snapshotItem && getMethodKey(snapshotItem.entity) === getMethodKey(method)) {
      // 如果追加数据，才更新data
      const listData = listDataGetter(rawData); // 更新data参数
      if (append) {
        // 下拉加载时需要替换当前页数据
        const dataRaw = _$(data);
        const pageSizeVal = _$(pageSize);

        // 当做移除操作时，替换的数量小于pageSize，此时dataRaw % pageSizeVal会大于0
        // 当新增操作时，替换的数量等于pageSize，此时dataRaw % pageSizeVal会等于0，此时不需要替换
        const replaceNumber = len(dataRaw) % pageSizeVal;
        replaceNumber > 0 &&
          upd$(data, rawd => {
            splice(rawd, (_$(page) - 1) * pageSizeVal, replaceNumber, ...listData);
            return rawd;
          });
      } else {
        upd$(data, listData);
      }
    }
  });
  states.onSuccess(({ data: rawData, sendArgs: [refreshPage, isRefresh], method }) => {
    const { total: cachedTotal } = getSnapshotMethods(method) || {};
    upd$(total, cachedTotal !== undefinedValue ? cachedTotal : totalGetter(rawData));
    if (!isRefresh) {
      fetchPreviousPage(rawData);
      fetchNextPage(rawData);
    }

    const pageSizeVal = _$(pageSize);
    const listData = listDataGetter(rawData); // 获取数组
    paginationAssert(isArray$1(listData), 'Got wrong array, did you return the correct array of list in `data` function');

    // 如果追加数据，才更新data
    if (append) {
      // 如果是reset则先清空数据
      isReset.current && upd$(data, []);
      if (refreshPage === undefinedValue) {
        upd$(data, [..._$(data), ...listData]);
      } else if (refreshPage) {
        // 如果是刷新页面，则是替换那一页的数据
        upd$(data, rawd => {
          splice(rawd, (refreshPage - 1) * pageSizeVal, pageSizeVal, ...listData);
          return rawd;
        });
      }
    } else {
      upd$(data, listData);
    }
  });

  // 请求成功与否，都要重置它们
  states.onComplete(() => {
    isReset.current = falseValue;
    requestCountInReseting.current = 0;
  });

  // 获取列表项所在位置
  const getItemIndex = item => {
    const index = _$(data).indexOf(item);
    paginationAssert(index >= 0, 'item is not found in list');
    return index;
  };

  /**
   * 刷新指定页码数据，此函数将忽略缓存强制发送请求
   * 如果未传入页码则会刷新当前页
   * 如果传入一个列表项，将会刷新此列表项所在页，只对append模式有效
   * @param pageOrItemPage 刷新的页码或列表项
   */
  const refresh = useMemorizedCallback$((pageOrItemPage = _$(page)) => {
    let refreshPage = pageOrItemPage;
    if (append) {
      if (!isNumber(pageOrItemPage)) {
        const itemIndex = getItemIndex(pageOrItemPage);
        refreshPage = Math.floor(itemIndex / _$(pageSize)) + 1;
      }
      paginationAssert(refreshPage <= _$(page), "refresh page can't greater than page");
      // 更新当前页数据
      promiseCatch(send(refreshPage, trueValue), noop$1);
    } else {
      paginationAssert(isNumber(refreshPage), 'unable to calculate refresh page by item in pagination mode');
      // 页数相等，则刷新当前页，否则fetch数据
      promiseCatch(
        refreshPage === _$(page)
          ? send(undefinedValue, trueValue)
          : fetch(handler(refreshPage, _$(pageSize)), trueValue),
        noop$1
      );
    }
  });

  const removeSyncRunner = useFlag$(createSyncOnceRunner()).current;
  // 删除除此usehook当前页和下一页的所有相关缓存
  const invalidatePaginationCache = (all = falseValue) => {
    const pageVal = _$(page);
    const snapshotObj = methodSnapshots();
    let snapshots = objectValues(snapshotObj);
    if (all) {
      removeSnapshot();
    } else {
      // 筛选出上一页、当前页、下一页的数据
      const excludeSnapshotKeys = map(
        filterItem(
          [getSnapshotMethods(pageVal - 1), getSnapshotMethods(pageVal), getSnapshotMethods(pageVal + 1)],
          Boolean
        ),
        ({ entity }) => getMethodKey(entity)
      );
      snapshots = map(
        filterItem(objectKeys(snapshotObj), key => !includes(excludeSnapshotKeys, key)),
        key => {
          const item = snapshotObj[key];
          delete snapshotObj[key];
          return item;
        }
      );
    }
    invalidateCache(map(snapshots, ({ entity }) => entity));
  };

  // 单独拿出来的原因是
  // 无论同步调用几次insert、remove，或它们组合调用，reset操作只需要异步执行一次
  const resetSyncRunner = useFlag$(createSyncOnceRunner()).current;
  const resetCache = () =>
    resetSyncRunner(() => {
      _$(fetchingRef) && abortFetch();
      // 缓存失效
      invalidatePaginationCache();

      // 当下一页的数据量不超过pageSize时，强制请求下一页，因为有请求共享，需要在中断请求后异步执行拉取操作
      setTimeoutFn(() => {
        const snapshotItem = getSnapshotMethods(_$(page) + 1);
        if (snapshotItem) {
          const cachedListData = listDataGetter(queryCache(snapshotItem.entity) || {}) || [];
          fetchNextPage(undefinedValue, len(cachedListData) < _$(pageSize));
        }
      });
    });

  // 统一更新总条数
  const updateTotal = offset => {
    // 更新当前页
    const totalVal = _$(total);
    if (isNumber(totalVal)) {
      const offsetedTotal = Math.max(totalVal + offset, 0);
      upd$(total, offsetedTotal);
      const pageVal = _$(page);

      // 更新冗余的total字段
      forEach([getSnapshotMethods(pageVal - 1), getSnapshotMethods(pageVal), getSnapshotMethods(pageVal + 1)], item => {
        item && (item.total = offsetedTotal);
      });
    }
  };

  /**
   * 插入一条数据
   * 如果未传入index，将默认插入到最前面
   * 如果传入一个列表项，将插入到这个列表项的后面，如果列表项未在列表数据中将会抛出错误
   * @param item 插入项
   * @param position 插入位置（索引）或列表项
   */
  const insert = useMemorizedCallback$((item, position = 0) => {
    const index = isNumber(position) ? position : getItemIndex(position) + 1;
    let popItem = undefinedValue;
    upd$(data, rawd => {
      // 当前展示的项数量刚好是pageSize的倍数时，才需要去掉一项数据，保证操作页的数量为pageSize
      if (len(_$(data)) % _$(pageSize) === 0) {
        popItem = rawd.pop();
      }
      // 插入位置为空默认插到最前面
      splice(rawd, index, 0, item);
      return rawd;
    });
    updateTotal(1);

    // 当前页的缓存同步更新
    updateCurrentPageCache();

    // 如果有pop项，将它放到下一页缓存的头部，与remove的操作保持一致
    // 这样在同步调用insert和remove时表现才一致
    if (popItem) {
      const snapshotItem = getSnapshotMethods(_$(page) + 1);
      snapshotItem &&
        setCache(snapshotItem.entity, rawData => {
          if (rawData) {
            const cachedListData = listDataGetter(rawData) || [];
            cachedListData.unshift(popItem);
            cachedListData.pop();
            return rawData;
          }
        });
    }

    // 插入项后也需要让缓存失效，以免不同条件下缓存未更新
    resetCache();
  });

  /**
   * 移除一条数据
   * 如果传入的是列表项，将移除此列表项，如果列表项未在列表数据中将会抛出错误
   * @param position 移除的索引或列表项
   */
  let tempData; // 临时保存的数据，以便当需要重新加载时用于数据恢复
  const remove = useMemorizedCallback$(position => {
    const index = isNumber(position) ? position : getItemIndex(position);
    indexAssert(index, _$(data));
    const pageVal = _$(page);
    const nextPage = pageVal + 1;
    const snapshotItem = getSnapshotMethods(nextPage);
    let fillingItem = undefinedValue; // 补位数据项
    snapshotItem &&
      setCache(snapshotItem.entity, rawData => {
        if (rawData) {
          const cachedListData = listDataGetter(rawData);
          // 从下一页列表的头部开始取补位数据
          fillingItem = shift(cachedListData || []);
          return rawData;
        }
      });

    const isLastPageVal = _$(isLastPage);
    if (fillingItem || isLastPageVal) {
      // 如果有下一页数据则通过缓存数据补位
      if (!tempData) {
        tempData = [..._$(data)];
      }
      if (index >= 0) {
        upd$(data, rawd => {
          splice(rawd, index, 1);
          // 如果有下一页的补位数据才去补位，因为有可能是最后一页才进入删除的
          fillingItem && pushItem(rawd, fillingItem);
          return rawd;
        });
      }
    } else if (tempData) {
      upd$(data, tempData); // 当移除项数都用完时还原数据，减少不必要的视图渲染
    }

    updateTotal(-1);
    // 当前页的缓存同步更新
    updateCurrentPageCache();

    // 如果没有下一页数据，或同步删除的数量超过了pageSize，则恢复数据并重新加载本页
    // 需异步操作，因为可能超过pageSize后还有remove函数被同步执行
    resetCache();
    removeSyncRunner(() => {
      // 移除最后一页数据时，就不需要再刷新了
      if (!fillingItem && !isLastPageVal) {
        refresh(pageVal);
      }
      if (!append && isLastPageVal && len(_$(data)) <= 0) {
        upd$(page, pageVal => pageVal - 1); // 翻页模式下，如果是最后一页且全部项被删除了，则往前翻一页
      }
      tempData = undefinedValue;
    });
  });

  /**
   * 替换一条数据
   * 如果position传入的是列表项，将替换此列表项，如果列表项未在列表数据中将会抛出错误
   * @param item 替换项
   * @param position 替换位置（索引）或列表项
   */
  const replace = useMemorizedCallback$((item, position) => {
    paginationAssert(position !== undefinedValue, 'must specify replace position');
    const index = isNumber(position) ? position : getItemIndex(position);
    indexAssert(index, _$(data));
    upd$(data, rawd => {
      splice(rawd, index, 1, item);
      return rawd;
    });
    // 当前页的缓存同步更新
    updateCurrentPageCache();
  });

  /**
   * 从第${initialPage}页开始重新加载列表，并清空缓存
   */
  const reload = useMemorizedCallback$(() => {
    invalidatePaginationCache(trueValue);
    isReset.current = trueValue;
    _$(page) === initialPage ? promiseCatch(send(), noop$1) : upd$(page, initialPage);
  });

  // 兼容react，每次缓存最新的操作函数，避免闭包陷阱
  delegationActions.current = {
    refresh,
    insert,
    remove,
    replace,
    reload
  };
  /** @Returns */
  return {
    ...states,

    /**
     * 将data的更新指向导出的data
     * @param newFrontStates 新状态对象
     */
    update: newFrontStates => {
      const { data: newData } = newFrontStates;
      if (newData) {
        upd$(data, newData);
        delete newFrontStates.data;
      }
      states.update(newFrontStates);
    },
    fetching: fetchStates.fetching,
    onFetchSuccess: fetchStates.onSuccess,
    onFetchError: fetchStates.onError,
    onFetchComplete: fetchStates.onComplete,

    page,
    pageSize,
    data: _exp$(data),
    pageCount: _exp$(pageCount),
    total: _exp$(total),
    isLastPage: _exp$(isLastPage),

    refresh,
    insert,
    remove,
    replace,
    reload
  };
}

var createHookEvent = (eventType, method, behavior, silentMethod, queueName, retryTimes, retryDelay, sendArgs, data, vDataResponse, error, status) => {
    const allPropsEvent = {
        /** 事件对应的请求行为 */
        behavior,
        /** 当前的method实例 */
        method,
        /** 当前的silentMethod实例，当behavior为static时没有值 */
        silentMethod,
        /** 已重试的次数，在beforePush和pushed事件中没有值 */
        retryTimes,
        /** 重试的延迟时间 */
        retryDelay,
        /** 通过send触发请求时传入的参数 */
        sendArgs,
        /** 响应数据，只在成功时有值 */
        data,
        /** 虚拟数据和实际值的集合 */
        vDataResponse,
        /** 失败时抛出的错误，只在失败时有值 */
        error,
        /** 响应状态 */
        status,
        /** silentMethod所在的队列名，全局事件有值 */
        queueName
    };
    const sqEvent = {};
    forEach(objectKeys(allPropsEvent), key => {
        allPropsEvent[key] !== undefinedValue &&
            (sqEvent[key] = allPropsEvent[key]);
    });
    // 将此类的对象重新命名，让它看上去是由不同的类生成的对象
    // 以此来对应typescript中定义的类型
    const typeName = [
        'GlobalSQEvent',
        'GlobalSQSuccessEvent',
        'GlobalSQErrorEvent',
        'GlobalSQFailEvent',
        'ScopedSQEvent',
        'ScopedSQSuccessEvent',
        'ScopedSQErrorEvent',
        'ScopedSQCompleteEvent',
        'ScopedSQRetryEvent',
        'RetriableRetryEvent',
        'RetriableFailEvent',
        'SSEOpenEvent',
        'SSEMessageEvent',
        'SSEErrorEvent' // 13
    ][eventType];
    typeName && defineProperty(sqEvent, symbolToStringTag, typeName);
    return sqEvent;
};

/**
 * 全局的虚拟数据收集数组
 * 它只会在method创建时为数组，其他时间为undefined
 *
 * 解释：收集虚拟数据的目的为了判断某个method实例内是否使用了虚拟数据
 * 包括以下形式：
 * useSQRequest((vDataId) => createMethod({ vDataId }) // 引用函数参数
 * useSQRequest(() => createMethod({ vDataId }) // 直接引用作用域参数
 *
 * 甚至是：
 * function createMethod(obj) {
 *   return alovaInst.Get('/list', {
 *     params: { status: obj.vDataId ? 1 : 0 }
 *   })
 * }
 * useSQRequest(() => createMethod(obj) // 直接引用作用域参数
 *
 * 使用虚拟数据的方式包含：
 * 1. 直接作为参数赋值
 * 2. 使用虚拟数据id
 * 3. 间接使用虚拟数据，如
 *    vData ? 1 : 0
 *    !!vData
 *    vData + 1
 *    等作为计算参数参与的形式
 */
let vDataIdCollectBasket;
const setVDataIdCollectBasket = (value) => (vDataIdCollectBasket = value);
/**
 * 依赖的alova实例，它的存储适配器、请求适配器等将用于存取SilentMethod实例，以及发送静默提交
 */
let dependentAlovaInstance;
const setDependentAlova = (alovaInst) => {
    dependentAlovaInstance = alovaInst;
};
/**
 * 设置自定义的序列化器
 */
let customSerializers = {};
const setCustomSerializers = (serializers = {}) => {
    customSerializers = serializers;
};
/**
 * silentFactory状态
 * 0表示未启动
 * 1表示进行中，调用bootSilentFactory后变更
 * 2表示请求失败，即按重试规则请求达到最大次数时，或不匹配重试规则时变更
 */
let silentFactoryStatus = 0;
const setSilentFactoryStatus = (status) => (silentFactoryStatus = status);
/**
 * silentQueue内的请求等待时间，单位为毫秒（ms）
 * 它表示即将发送请求的silentMethod的等待时间
 * 如果未设置，或设置为0表示立即触发silentMethod请求
 *
 * Tips:
 * 1. 直接设置时默认对default queue有效
 * 2. 如果需要对其他queue设置可指定为对象，如：
 * [
 *   表示对名为customName的队列设置请求等待5000ms
 *   { name: 'customName', wait: 5000 },
 *
 *   // 表示前缀为prefix的所有队列中，method实例名为xxx的请求设置等待5000ms
 *   { name: /^prefix/, wait: silentMethod => silentMethod.entity.config.name === 'xxx' ? 5000 : 0 },
 * ]
 *
 * >>> 它只在请求成功时起作用，如果失败则会使用重试策略参数
 */
let queueRequestWaitSetting = [];
const setQueueRequestWaitSetting = (requestWaitSetting = 0) => {
    queueRequestWaitSetting = isArray$1(requestWaitSetting)
        ? requestWaitSetting
        : [
            {
                queue: DEFAUT_QUEUE_NAME,
                wait: requestWaitSetting
            }
        ];
};
/** 全局的silent事件回调函数 */
const bootHandlers = [];
const beforeHandlers = [];
const successHandlers = [];
const errorHandlers = [];
const failHandlers = [];
/** silentAssert */
const silentAssert = createAssert('useSQHook');

var dateSerializer = {
    forward: data => (instanceOf(data, Date) ? data.getTime() : undefinedValue),
    backward: ts => newInstance(Date, ts)
};

var regexpSerializer = {
    forward: data => (instanceOf(data, RegExpCls) ? data.source : undefined),
    backward: source => newInstance(RegExpCls, source)
};

const createSerializerPerformer = (customSerializers = {}) => {
    /**
     * 合并内置序列化器和自定义序列化器
     */
    const serializers = {
        date: dateSerializer,
        regexp: regexpSerializer,
        ...customSerializers
    };
    /**
     * 序列化数据
     */
    const serialize = (payload) => {
        if (isObject(payload)) {
            payload = walkObject(isArray$1(payload) ? [...payload] : { ...payload }, value => {
                let finallyApplySerializerName = undefinedValue;
                // 找到匹配的序列化器并进行值的序列化，未找到则返回原值
                const serializedValue = objectKeys(serializers).reduce((currentValue, serializerName) => {
                    if (!finallyApplySerializerName) {
                        const serializedValueItem = serializers[serializerName].forward(currentValue);
                        if (serializedValueItem !== undefinedValue) {
                            finallyApplySerializerName = serializerName;
                            currentValue = serializedValueItem;
                        }
                    }
                    return currentValue;
                }, value);
                // 需要用原始值判断，否则像new Number(1)等包装类也会是[object Object]
                const toStringTag = ObjectCls.prototype.toString.call(value);
                if (toStringTag === '[object Object]') {
                    value = { ...value };
                }
                else if (isArray$1(value)) {
                    value = [...value];
                }
                return finallyApplySerializerName !== undefinedValue ? [finallyApplySerializerName, serializedValue] : value;
            });
        }
        return payload;
    };
    /**
     * 反序列化数据
     */
    const deserialize = (payload) => isObject(payload)
        ? walkObject(payload, value => {
            if (isArray$1(value) && len(value) === 2) {
                const foundSerializer = serializers[value[0]];
                value = foundSerializer ? foundSerializer.backward(value[1]) : value;
            }
            return value;
        }, falseValue)
        : payload;
    return {
        serialize,
        deserialize
    };
};

const symbolVDataId = Symbol('vdid'), symbolOriginal = Symbol('original'), regVDataId = /\[vd:([0-9a-z]+)\]/;

/**
 * 统一的vData收集函数
 * 它将在以下4个位置被调用
 * 1. 访问子属性时
 * 2. 参与计算并触发[Symbol.toPrimitive]时
 * 3. 获取vData的id时
 * 4. 获取其原始值时
 *
 * @param returnValue 返回值，如果是函数则调用它
 * @returns 收集函数
 */
const vDataCollectUnified = (target) => {
    const vDataId = target === null || target === void 0 ? void 0 : target[symbolVDataId];
    vDataId && vDataIdCollectBasket && (vDataIdCollectBasket[vDataId] = undefinedValue);
};
// export const vDataGetter = (key: string) => vDataCollectGetter((thisObj: any) => thisObj.__proto__[key].call(thisObj));

/**
 * 虚拟数据字符串化，如果参数不是虚拟数据则返回原数据
 * @param target 虚拟数据
 * @param returnOriginalIfNotVData 如果不是虚拟数据则返回原值
 * @returns 虚拟数据id或原数据
 */
const stringifyVData = (target, returnOriginalIfNotVData = trueValue) => {
    vDataCollectUnified(target);
    const vDataIdRaw = target === null || target === void 0 ? void 0 : target[symbolVDataId];
    const vDataId = vDataIdRaw ? `[vd:${vDataIdRaw}]` : undefinedValue;
    return vDataId || (returnOriginalIfNotVData ? target : undefinedValue);
};
/**
 * 创建虚拟数据id收集的getter函数
 * @param valueReturnFn 返回值函数
 * @returns getter函数
 */
function stringifyWithThis() {
    return stringifyVData(this);
}

/**
 * Null包装类实现
 */
const Null = function () { };
Null.prototype = ObjectCls.create(nullValue, {
    [STR_VALUE_OF]: valueObject(stringifyWithThis)
});

/**
 * Undefined包装类实现
 */
const Undefined = function () { };
Undefined.prototype = ObjectCls.create(nullValue, {
    [STR_VALUE_OF]: valueObject(stringifyWithThis)
});

/**
 * 创建虚拟响应数据
 * @returns 虚拟响应数据代理实例
 */
var createVirtualResponse = (structure, vDataId = uuid()) => {
    const transform2VData = (value, vDataIdInner = uuid()) => {
        if (value === nullValue) {
            value = newInstance(Null);
        }
        else if (value === undefinedValue) {
            value = newInstance(Undefined);
        }
        else {
            const newValue = ObjectCls(value);
            defineProperty(newValue, STR_VALUE_OF, stringifyWithThis);
            defineProperty(newValue, symbolOriginal, value);
            value = newValue;
        }
        defineProperty(value, symbolVDataId, vDataIdInner);
        return value;
    };
    const virtualResponse = transform2VData(structure, vDataId);
    if (isPlainOrCustomObject(virtualResponse) || isArray$1(virtualResponse)) {
        walkObject(virtualResponse, value => transform2VData(value));
    }
    return virtualResponse;
};

!!(process.env.NODE_ENV !== "production") ? Object.freeze({}) : {};
!!(process.env.NODE_ENV !== "production") ? Object.freeze([]) : [];
const isArray = Array.isArray;

/**
 * 获取带虚拟数据变量的原始值
 * 此函数也将会进行vData收集
 * @param target 目标值
 * @param deepDehydrate 是否深度脱水值
 * @returns 具有原始类型的目标值
 */
const dehydrateVDataUnified = (target, deepDehydrate = trueValue) => {
    const dehydrateItem = (value) => {
        vDataCollectUnified(value);
        if (value === null || value === void 0 ? void 0 : value[symbolVDataId]) {
            if (instanceOf(value, Undefined)) {
                value = undefinedValue;
            }
            else if (instanceOf(value, Null)) {
                value = nullValue;
            }
            else if (instanceOf(value, NumberCls) || instanceOf(value, StringCls) || instanceOf(value, BooleanCls)) {
                value = value[symbolOriginal];
            }
        }
        return value;
    };
    const newTarget = dehydrateItem(target);
    // 如果是对象或数组，需要深度遍历获取虚拟数据值
    if (deepDehydrate && (isPlainOrCustomObject(newTarget) || isArray(newTarget))) {
        walkObject(newTarget, value => dehydrateItem(value));
    }
    return newTarget;
};
/**
 * 上面函数deepDehydrate为true的版本
 */
var dehydrateVData = (target) => dehydrateVDataUnified(target);

const vDataKey = '__$k', vDataValueKey = '__$v';
const getAlovaStorage = () => {
    // 未启动silentFactory时提供提示
    silentAssert(!!dependentAlovaInstance, 'alova instance is not found, Do you forget to set `alova` or call `bootSilentFactory`?');
    return dependentAlovaInstance.storage;
};
let serializerPerformer = undefinedValue;
const silentMethodIdQueueMapStorageKey = 'alova.SQ', // silentMethod实例id组成的队列集合缓存key
silentMethodStorageKeyPrefix = 'alova.SM.', // silentMethod实例缓存key前缀
/**
 * 持久化带虚拟数据和可序列化的数据集合
 * @param key 持久化key
 * @param payload 持久化数据
 */
storageSetItem = (key, payload) => {
    const storage = getAlovaStorage();
    if (isObject(payload)) {
        payload = walkObject(isArray$1(payload) ? [...payload] : { ...payload }, (value, key, parent) => {
            var _a;
            if (key === vDataValueKey && parent[vDataKey]) {
                return value;
            }
            // 如果序列化的是silentMethod实例，则过滤掉alova实例
            if (key === 'context' && ((_a = value === null || value === void 0 ? void 0 : value.constructor) === null || _a === void 0 ? void 0 : _a.name) === 'Alova') {
                return undefinedValue;
            }
            const vDataId = value === null || value === void 0 ? void 0 : value[symbolVDataId];
            let primitiveValue = dehydrateVDataUnified(value, falseValue);
            // 需要用原始值判断，否则像new Number(1)等包装类也会是[object Object]
            const toStringTag = ObjectCls.prototype.toString.call(primitiveValue);
            if (toStringTag === '[object Object]') {
                value = { ...value };
                primitiveValue = {};
            }
            else if (isArray$1(value)) {
                value = [...value];
                primitiveValue = [];
            }
            if (vDataId) {
                const valueWithVData = {
                    [vDataKey]: vDataId,
                    // 对于对象和数组来说，它内部的属性会全部通过`...value`放到外部，因此内部的不需要再进行遍历转换了
                    // 因此将数组或对象置空，这样既避免了重复转换，又避免了污染原对象
                    [vDataValueKey]: primitiveValue,
                    ...value
                };
                // 如果是String类型，将会有像数组一样的如0、1、2为下标，值为字符的项，需将他们过滤掉
                if (instanceOf(value, StringCls)) {
                    for (let i = 0; i < len(value); i++) {
                        valueWithVData === null || valueWithVData === void 0 ? true : delete valueWithVData[i];
                    }
                }
                // 如果转换成了虚拟数据，则将转换值赋给它内部，并在下面逻辑中统一由value处理
                value = valueWithVData;
            }
            return value;
        });
    }
    serializerPerformer = serializerPerformer || createSerializerPerformer(customSerializers);
    storage.set(key, serializerPerformer.serialize(payload));
}, 
/**
 * 取出持久化数据，并数据转换虚拟数据和已序列化数据
 * @param key 持久化数据的key
 */
storageGetItem = (key) => {
    const storagedResponse = getAlovaStorage().get(key);
    serializerPerformer = serializerPerformer || createSerializerPerformer(customSerializers);
    return isObject(storagedResponse)
        ? walkObject(serializerPerformer.deserialize(storagedResponse), value => {
            // 将虚拟数据格式转换回虚拟数据实例
            if (isObject(value) && (value === null || value === void 0 ? void 0 : value[vDataKey])) {
                const vDataId = value[vDataKey];
                const vDataValue = createVirtualResponse(value[vDataValueKey], vDataId);
                forEach(objectKeys(value), key => {
                    if (!includes([vDataKey, vDataValueKey], key)) {
                        vDataValue[key] = value[key];
                    }
                });
                value = vDataValue;
            }
            return value;
        }, falseValue)
        : storagedResponse;
}, 
/**
 * 移除持久化数据
 * @param key 持久化数据的key
 */
storageRemoveItem = (key) => {
    getAlovaStorage().remove(key);
};

/**
 * 序列化并保存silentMethod实例
 * @param silentMethodInstance silentMethod实例
 */
const persistSilentMethod = (silentMethodInstance) => {
    storageSetItem(silentMethodStorageKeyPrefix + silentMethodInstance.id, silentMethodInstance);
};
/**
 * 将静默请求的配置信息放入对应storage中
 * 逻辑：通过构造一个key，并用这个key将静默方法的配置信息放入对应storage中，然后将key存入统一管理key的存储中
 * @param silentMethod SilentMethod实例
 * @param queue 操作的队列名
 */
const push2PersistentSilentQueue = (silentMethodInstance, queueName) => {
    persistSilentMethod(silentMethodInstance);
    // 将silentMethod实例id保存到queue存储中
    const silentMethodIdQueueMap = (storageGetItem(silentMethodIdQueueMapStorageKey) ||
        {});
    const currentQueue = (silentMethodIdQueueMap[queueName] = silentMethodIdQueueMap[queueName] || []);
    pushItem(currentQueue, silentMethodInstance.id);
    storageSetItem(silentMethodIdQueueMapStorageKey, silentMethodIdQueueMap);
};
/**
 * 对缓存中的silentMethod实例移除或替换
 * @param queue 操作的队列名
 * @param targetSilentMethodId 目标silentMethod实例id
 * @param newSilentMethod 替换的新silentMethod实例，未传则表示删除
 */
const spliceStorageSilentMethod = (queueName, targetSilentMethodId, newSilentMethod) => {
    // 将silentMethod实例id从queue中移除
    const silentMethodIdQueueMap = (storageGetItem(silentMethodIdQueueMapStorageKey) ||
        {});
    const currentQueue = silentMethodIdQueueMap[queueName] || [];
    const index = currentQueue.findIndex(id => id === targetSilentMethodId);
    if (index >= 0) {
        if (newSilentMethod) {
            splice(currentQueue, index, 1, newSilentMethod.id);
            persistSilentMethod(newSilentMethod);
        }
        else {
            splice(currentQueue, index, 1);
        }
        storageRemoveItem(silentMethodStorageKeyPrefix + targetSilentMethodId);
        // 队列为空时删除此队列
        len(currentQueue) <= 0 && delete silentMethodIdQueueMap[queueName];
        if (len(objectKeys(silentMethodIdQueueMap)) > 0) {
            storageSetItem(silentMethodIdQueueMapStorageKey, silentMethodIdQueueMap);
        }
        else {
            // 队列集合为空时移除它
            storageRemoveItem(silentMethodIdQueueMapStorageKey);
        }
    }
};

/** 静默方法队列集合 */
let silentQueueMap = {};
/**
 * 合并queueMap到silentMethod队列集合
 * @param queueMap silentMethod队列集合
 */
const merge2SilentQueueMap = (queueMap) => {
    forEach(objectKeys(queueMap), targetQueueName => {
        const currentQueue = (silentQueueMap[targetQueueName] = silentQueueMap[targetQueueName] || []);
        pushItem(currentQueue, ...queueMap[targetQueueName]);
    });
};
/**
 * 深层遍历目标数据，并将虚拟数据替换为实际数据
 * @param target 目标数据
 * @param vDataResponse 虚拟数据和实际数据的集合
 * @returns 是否有替换数据
 */
const deepReplaceVData = (target, vDataResponse) => {
    // 搜索单一值并将虚拟数据对象或虚拟数据id替换为实际值
    const replaceVData = (value) => {
        const vData = stringifyVData(value);
        // 如果直接是虚拟数据对象并且在vDataResponse中，则使用vDataResponse中的值替换Map
        // 如果是字符串，则里面可能包含虚拟数据id并且在vDataResponse中，也需将它替换为实际值Map
        // 不在本次vDataResponse中的虚拟数据将不变，它可能是下一次请求的虚拟数据Map
        if (vData in vDataResponse) {
            return vDataResponse[vData];
        }
        else if (isString(value)) {
            return value.replace(newInstance(RegExpCls, regVDataId.source, 'g'), mat => mat in vDataResponse ? vDataResponse[mat] : mat);
        }
        return value;
    };
    if (isObject(target) && !stringifyVData(target, falseValue)) {
        walkObject(target, replaceVData);
    }
    else {
        target = replaceVData(target);
    }
    return target;
};
/**
 * 更新队列内的method实例，将虚拟数据替换为实际数据
 * @param vDataResponse 虚拟id和对应真实数据的集合
 * @param targetQueue 目标队列
 */
const updateQueueMethodEntities = (vDataResponse, targetQueue) => {
    forEach(targetQueue, silentMethodItem => {
        // 深层遍历entity对象，如果发现有虚拟数据或虚拟数据id，则替换为实际数据
        deepReplaceVData(silentMethodItem.entity, vDataResponse);
        // 如果method实例有更新，则重新持久化此silentMethod实例
        silentMethodItem.cache && persistSilentMethod(silentMethodItem);
    });
};
/**
 * 使用响应数据替换虚拟数据
 * @param response 真实响应数据
 * @param virtualResponse 虚拟响应数据
 * @returns 虚拟数据id所构成的对应真实数据集合
 */
const replaceVirtualResponseWithResponse = (virtualResponse, response) => {
    let vDataResponse = {};
    const vDataId = stringifyVData(virtualResponse, falseValue);
    vDataId && (vDataResponse[vDataId] = response);
    if (isObject(virtualResponse)) {
        for (const i in virtualResponse) {
            vDataResponse = {
                ...vDataResponse,
                ...replaceVirtualResponseWithResponse(virtualResponse[i], response === null || response === void 0 ? void 0 : response[i])
            };
        }
    }
    return vDataResponse;
};
/**
 * 启动SilentMethod队列
 * 1. 静默提交将会放入队列中，并按顺序发送请求，只有等到前一个请求响应后才继续发送后面的请求
 * 2. 重试次数只有在未响应时才触发，在服务端响应错误或断网情况下，不会重试
 * 3. 在达到重试次数仍未成功时，当设置了nextRound（下一轮）时延迟nextRound指定的时间后再次请求，否则将在刷新后再次尝试
 * 4. 如果有resolveHandler和rejectHandler，将在请求完成后（无论成功还是失败）调用，通知对应的请求继续响应
 *
 * @param queue SilentMethod队列
 */
const setSilentMethodActive = (silentMethodInstance, active) => {
    if (active) {
        silentMethodInstance.active = active;
    }
    else {
        delete silentMethodInstance.active;
    }
};
const defaultBackoffDelay = 1000;
const bootSilentQueue = (queue, queueName) => {
    /**
     * 根据请求等待参数控制回调函数的调用，如果未设置或小于等于0则立即触发
     * @param queueName 队列名称
     * @param callback 回调函数
     */
    const emitWithRequestDelay = (queueName) => {
        const nextSilentMethod = queue[0];
        if (nextSilentMethod) {
            const targetSetting = queueRequestWaitSetting.find(({ queue }) => instanceOf(queue, RegExpCls) ? regexpTest(queue, queueName) : queue === queueName);
            const callback = () => queue[0] && silentMethodRequest(queue[0]);
            const delay = (targetSetting === null || targetSetting === void 0 ? void 0 : targetSetting.wait) ? sloughConfig(targetSetting.wait, [nextSilentMethod, queueName]) : 0;
            delay && delay > 0 ? setTimeoutFn(callback, delay) : callback();
        }
    };
    /**
     * 运行单个silentMethod实例
     * @param silentMethodInstance silentMethod实例
     * @param retryTimes 重试的次数
     */
    const silentMethodRequest = (silentMethodInstance, retryTimes = 0) => {
        // 将当前silentMethod实例设置活跃状态
        setSilentMethodActive(silentMethodInstance, trueValue);
        const { cache, id, behavior, entity, retryError = /.*/, maxRetryTimes = 0, backoff = { delay: defaultBackoffDelay }, resolveHandler = noop$1, rejectHandler = noop$1, fallbackHandlers = [], retryHandlers = [], handlerArgs = [], virtualResponse, force } = silentMethodInstance;
        // 触发请求前事件
        runArgsHandler(beforeHandlers, createHookEvent(0, entity, behavior, silentMethodInstance, queueName, retryTimes));
        promiseThen(entity.send(force), data => {
            // 请求成功，移除成功的silentMethod实力，并继续下一个请求
            shift(queue);
            // 请求成功，把成功的silentMethod实例在storage中移除，并继续下一个请求
            cache && spliceStorageSilentMethod(queueName, id);
            // 如果有resolveHandler则调用它通知外部
            resolveHandler(data);
            // 有virtualResponse时才遍历替换虚拟数据，且触发全局事件
            // 一般为silent behavior，而queue behavior不需要
            if (behavior === BEHAVIOR_SILENT) {
                // 替换队列中后面方法实例中的虚拟数据为真实数据
                // 开锁后才能正常访问virtualResponse的层级结构
                const vDataResponse = replaceVirtualResponseWithResponse(virtualResponse, data);
                const { targetRefMethod, updateStates } = silentMethodInstance; // 实时获取才准确
                // 如果此silentMethod带有targetRefMethod，则再次调用updateState更新数据
                // 此为延迟数据更新的实现
                if (instanceOf(targetRefMethod, Method) && updateStates && len(updateStates) > 0) {
                    const updateStateCollection = {};
                    forEach(updateStates, stateName => {
                        // 请求成功后，将带有虚拟数据的数据替换为实际数据
                        updateStateCollection[stateName] = dataRaw => deepReplaceVData(dataRaw, vDataResponse);
                    });
                    const updated = updateState(targetRefMethod, updateStateCollection);
                    // 修改状态不成功，则去修改缓存数据
                    if (!updated) {
                        setCache(targetRefMethod, (dataRaw) => deepReplaceVData(dataRaw, vDataResponse));
                    }
                }
                // 对当前队列的后续silentMethod实例进行虚拟数据替换
                updateQueueMethodEntities(vDataResponse, queue);
                // 触发全局的成功事件
                runArgsHandler(successHandlers, createHookEvent(1, entity, behavior, silentMethodInstance, queueName, retryTimes, undefinedValue, undefinedValue, data, vDataResponse));
            }
            // 设为非激活状态
            setSilentMethodActive(silentMethodInstance, falseValue);
            // 继续下一个silentMethod的处理
            emitWithRequestDelay(queueName);
        }, reason => {
            if (behavior !== BEHAVIOR_SILENT) {
                // 当behavior不为silent时，请求失败就触发rejectHandler
                // 且在队列中移除，并不再重试
                shift(queue);
                rejectHandler(reason);
            }
            else {
                // 每次请求错误都将触发错误回调
                const runGlobalErrorEvent = (retryDelay) => runArgsHandler(errorHandlers, createHookEvent(2, entity, behavior, silentMethodInstance, queueName, retryTimes, retryDelay, undefinedValue, undefinedValue, undefinedValue, reason));
                // 在silent行为模式下，判断是否需要重试
                // 重试只有在响应错误符合retryError正则匹配时有效
                const { name: errorName = '', message: errorMsg = '' } = reason || {};
                let regRetryErrorName = void 0, regRetryErrorMsg = void 0;
                if (instanceOf(retryError, RegExp)) {
                    regRetryErrorMsg = retryError;
                }
                else if (isObject(retryError)) {
                    regRetryErrorName = retryError.name;
                    regRetryErrorMsg = retryError.message;
                }
                const matchRetryError = (regRetryErrorName && regexpTest(regRetryErrorName, errorName)) ||
                    (regRetryErrorMsg && regexpTest(regRetryErrorMsg, errorMsg));
                // 如果还有重试次数则进行重试
                if (retryTimes < maxRetryTimes && matchRetryError) {
                    // 需要使用下次的retryTimes来计算延迟时间，因此这边需+1
                    const retryDelay = delayWithBackoff(backoff, retryTimes + 1);
                    runGlobalErrorEvent(retryDelay);
                    setTimeoutFn(() => {
                        silentMethodRequest(silentMethodInstance, ++retryTimes);
                        runArgsHandler(retryHandlers, createHookEvent(8, entity, behavior, silentMethodInstance, undefinedValue, retryTimes, retryDelay, handlerArgs));
                    }, 
                    // 还有重试次数时使用timeout作为下次请求时间
                    retryDelay);
                }
                else {
                    setSilentFactoryStatus(2);
                    runGlobalErrorEvent();
                    // 达到失败次数，或不匹配重试的错误信息时，触发失败回调
                    runArgsHandler(fallbackHandlers, createHookEvent(6, entity, behavior, silentMethodInstance, undefinedValue, undefinedValue, undefinedValue, handlerArgs, undefinedValue, undefinedValue, reason));
                    runArgsHandler(failHandlers, createHookEvent(3, entity, behavior, silentMethodInstance, queueName, retryTimes, undefinedValue, undefinedValue, undefinedValue, undefinedValue, reason));
                }
            }
            // 设为非激活状态
            setSilentMethodActive(silentMethodInstance, falseValue);
        });
    };
    emitWithRequestDelay(queueName);
};
/**
 * 将新的silentMethod实例放入队列中
 * @param silentMethodInstance silentMethod实例
 * @param cache silentMethod是否有缓存
 * @param targetQueueName 目标队列名
 * @param onBeforePush silentMethod实例push前的事件
 */
const pushNewSilentMethod2Queue = (silentMethodInstance, cache, targetQueueName = DEFAUT_QUEUE_NAME, onBeforePush = noop$1) => {
    silentMethodInstance.cache = cache;
    const currentQueue = (silentQueueMap[targetQueueName] = silentQueueMap[targetQueueName] || []);
    const isNewQueue = len(currentQueue) <= 0;
    const isPush2Queue = onBeforePush() !== falseValue;
    // silent行为下，如果没有绑定fallback事件回调，则持久化
    // 如果在onBeforePushQueue返回false，也不再放入队列中
    if (isPush2Queue) {
        cache && push2PersistentSilentQueue(silentMethodInstance, targetQueueName);
        pushItem(currentQueue, silentMethodInstance);
        // 如果是新的队列且状态为已启动，则执行它
        isNewQueue && silentFactoryStatus === 1 && bootSilentQueue(currentQueue, targetQueueName);
    }
    return isPush2Queue;
};

/**
 * 定位silentMethod实例所在的位置
 * @param silentMethodInstance silentMethod实例
 */
const getBelongQueuePosition = (silentMethodInstance) => {
    let queue = undefinedValue;
    let queueName = '';
    let position = 0;
    for (const queueNameLoop in silentQueueMap) {
        position = silentQueueMap[queueNameLoop].indexOf(silentMethodInstance);
        if (position >= 0) {
            queue = silentQueueMap[queueNameLoop];
            queueName = queueNameLoop;
            break;
        }
    }
    return [queue, queueName, position];
};
/**
 * silentMethod实例
 * 需要进入silentQueue的请求都将被包装成silentMethod实例，它将带有请求策略的各项参数
 */
class SilentMethod {
    constructor(entity, behavior, id = uuid(), force, retryError, maxRetryTimes, backoff, fallbackHandlers, resolveHandler, rejectHandler, handlerArgs, vDatas, retryHandlers) {
        const thisObj = this;
        thisObj.entity = entity;
        thisObj.behavior = behavior;
        thisObj.id = id;
        thisObj.force = !!force;
        thisObj.retryError = retryError;
        thisObj.maxRetryTimes = maxRetryTimes;
        thisObj.backoff = backoff;
        thisObj.fallbackHandlers = fallbackHandlers;
        thisObj.resolveHandler = resolveHandler;
        thisObj.rejectHandler = rejectHandler;
        thisObj.handlerArgs = handlerArgs;
        thisObj.vDatas = vDatas;
        thisObj.retryHandlers = retryHandlers;
    }
    /**
     * 允许缓存时持久化更新当前实例
     */
    save() {
        this.cache && persistSilentMethod(this);
    }
    /**
     * 在队列中使用一个新的silentMethod实例替换当前实例
     * 如果有持久化缓存也将会更新缓存
     * @param newSilentMethod 新的silentMethod实例
     */
    replace(newSilentMethod) {
        const targetSilentMethod = this;
        silentAssert(newSilentMethod.cache === targetSilentMethod.cache, 'the cache of new silentMethod must equal with this silentMethod');
        const [queue, queueName, position] = getBelongQueuePosition(targetSilentMethod);
        if (queue) {
            splice(queue, position, 1, newSilentMethod);
            targetSilentMethod.cache && spliceStorageSilentMethod(queueName, targetSilentMethod.id, newSilentMethod);
        }
    }
    /**
     * 移除当前实例，如果有持久化数据，也会同步移除
     */
    remove() {
        const targetSilentMethod = this;
        const [queue, queueName, position] = getBelongQueuePosition(targetSilentMethod);
        if (queue) {
            splice(queue, position, 1);
            targetSilentMethod.cache && spliceStorageSilentMethod(queueName, targetSilentMethod.id);
        }
    }
    /**
     * 设置延迟更新状态对应的method实例以及对应的状态名
     * 它将在此silentMethod响应后，找到对应的状态数据并将vData更新为实际数据
     *
     * @param matcher method实例匹配器
     * @param updateStateName 更新的状态名，默认为data，也可以设置多个
     */
    setUpdateState(matcher, updateStateName = 'data') {
        const methodInstance = instanceOf(matcher, Method)
            ? matcher
            : matchSnapshotMethod(matcher, falseValue);
        if (methodInstance) {
            this.targetRefMethod = methodInstance;
            this.updateStates = isArray$1(updateStateName) ? updateStateName : [updateStateName];
        }
    }
}

/**
 * 全局的silentMethod实例，它将在第一个成功事件触发前到最后一个成功事件触发后有值（同步时段）
 * 通过此方式让onSuccess中的updateStateEffect内获得当前的silentMethod实例
 */
let currentSilentMethod = undefinedValue;
/**
 * 创建SilentQueue中间件函数
 * @param config 配置对象
 * @returns 中间件函数
 */
var createSilentQueueMiddlewares = (handler, config) => {
    const { behavior = 'queue', queue, retryError, maxRetryTimes, backoff } = config || {};
    const fallbackHandlers = [];
    const beforePushQueueHandlers = [];
    const pushedQueueHandlers = [];
    const retryHandlers = [];
    let handlerArgs;
    /**
     * method实例创建函数
     * @param {any[]} args 调用send传入的函数
     * @returns {Method}
     */
    const createMethod = (...args) => {
        silentAssert(isFn(handler), 'method handler must be a function. eg. useSQRequest(() => method)');
        setVDataIdCollectBasket({});
        handlerArgs = args;
        return handler(...args);
    };
    /**
     * 中间件函数
     * @param context 请求上下文，包含请求相关的值
     * @param next 继续执行函数
     * @returns {Promise}
     */
    const middleware = ({ method, sendArgs, cachedResponse, update, decorateSuccess, decorateError, decorateComplete, config }, next) => {
        const { silentDefaultResponse, vDataCaptured, force = falseValue } = config;
        // 因为behavior返回值可能会变化，因此每次请求都应该调用它重新获取返回值
        const behaviorFinally = sloughConfig(behavior, sendArgs);
        const queueFinally = sloughConfig(queue, sendArgs);
        const forceRequest = sloughConfig(force, sendArgs);
        let silentMethodInstance;
        // 设置事件回调装饰器
        decorateSuccess((handler, event, index, length) => {
            if (index === 0) {
                currentSilentMethod = silentMethodInstance;
            }
            handler(createHookEvent(5, method, behaviorFinally, silentMethodInstance, undefinedValue, undefinedValue, undefinedValue, event.sendArgs, event.data));
            // 所有成功回调执行完后再锁定虚拟数据，锁定后虚拟响应数据内不能再访问任意层级
            // 锁定操作只在silent模式下，用于锁定虚拟数据的生成操作
            if (index === length - 1) {
                currentSilentMethod = undefinedValue;
            }
        });
        decorateError((handler, event) => {
            handler(createHookEvent(6, method, behaviorFinally, silentMethodInstance, undefinedValue, undefinedValue, undefinedValue, event.sendArgs, undefinedValue, undefinedValue, event.error));
        });
        decorateComplete((handler, event) => {
            handler(createHookEvent(7, method, behaviorFinally, silentMethodInstance, undefinedValue, undefinedValue, undefinedValue, event.sendArgs, event.data, undefinedValue, event.error, event.status));
        });
        // 置空临时收集变量
        // 返回前都需要置空它们
        const resetCollectBasket = () => {
            setVDataIdCollectBasket((handlerArgs = undefinedValue));
        };
        // 如果设置了vDataCaptured，则先判断请求相关的数据是否包含虚拟数据
        if (isFn(vDataCaptured)) {
            let hasVData = vDataIdCollectBasket && len(objectKeys(vDataIdCollectBasket)) > 0;
            if (!hasVData) {
                const { url, data } = method;
                const { params, headers } = getConfig(method);
                walkObject({ url, params, data, headers }, value => {
                    if (!hasVData && (stringifyVData(value, falseValue) || regexpTest(regVDataId, value))) {
                        hasVData = trueValue;
                    }
                    return value;
                });
            }
            // 如果vDataCaptured有返回数据，则使用它作为响应数据，否则继续请求
            const customResponse = hasVData ? vDataCaptured(method) : undefinedValue;
            if (customResponse !== undefinedValue) {
                resetCollectBasket(); // 被vDataCaptured捕获时的重置
                return promiseResolve(customResponse);
            }
        }
        if (behaviorFinally !== BEHAVIOR_STATIC) {
            // 等待队列中的method执行完毕
            const createSilentMethodPromise = () => {
                const queueResolvePromise = newInstance(PromiseCls, (resolveHandler, rejectHandler) => {
                    silentMethodInstance = newInstance(SilentMethod, method, behaviorFinally, undefinedValue, !!forceRequest, retryError, maxRetryTimes, backoff, fallbackHandlers, resolveHandler, rejectHandler, handlerArgs, vDataIdCollectBasket && objectKeys(vDataIdCollectBasket), retryHandlers);
                    resetCollectBasket(); // behavior为queue和silent时的重置
                });
                // onBeforePush和onPushed事件是同步绑定的，因此需要异步执行入队列才能正常触发事件
                promiseThen(promiseResolve(), () => {
                    const createPushEvent = () => createHookEvent(4, method, behaviorFinally, silentMethodInstance, undefinedValue, undefinedValue, undefinedValue, sendArgs);
                    // 将silentMethod放入队列并持久化
                    const isPushed = pushNewSilentMethod2Queue(silentMethodInstance, 
                    // onFallback绑定了事件后，即使是silent行为模式也不再存储
                    // onFallback会同步调用，因此需要异步判断是否存在fallbackHandlers
                    len(fallbackHandlers) <= 0 && behaviorFinally === BEHAVIOR_SILENT, queueFinally, 
                    // 执行放入队列前回调，如果返回false则阻止放入队列
                    () => runArgsHandler(beforePushQueueHandlers, createPushEvent()));
                    // 只有在放入队列后，才执行放入队列后的回调
                    isPushed && runArgsHandler(pushedQueueHandlers, createPushEvent());
                });
                return queueResolvePromise;
            };
            if (behaviorFinally === BEHAVIOR_QUEUE) {
                const forceRequest = sloughConfig(force, sendArgs);
                // 强制请求，或命中缓存时需要更新loading状态
                const needSendRequest = forceRequest || !cachedResponse;
                if (needSendRequest) {
                    update({
                        // 手动设置为true
                        loading: trueValue
                    });
                }
                // 当使用缓存时，直接使用缓存，否则再进入请求队列
                return needSendRequest ? createSilentMethodPromise() : promiseThen(promiseResolve(cachedResponse));
            }
            const silentMethodPromise = createSilentMethodPromise();
            // 在silent模式下创建虚拟响应数据，虚拟响应数据可生成任意的虚拟数据
            const virtualResponse = (silentMethodInstance.virtualResponse = createVirtualResponse(isFn(silentDefaultResponse) ? silentDefaultResponse() : undefinedValue));
            promiseThen(silentMethodPromise, realResponse => {
                // 获取到真实数据后更新过去
                update({
                    data: realResponse
                });
            });
            // silent模式下，先立即返回虚拟响应值，然后当真实数据返回时再更新
            return promiseResolve(virtualResponse);
        }
        resetCollectBasket(); // behavior为static时的重置
        return next();
    };
    return {
        c: createMethod,
        m: middleware,
        // 事件绑定函数
        b: {
            /**
             * 绑定回退事件
             * @param handler 回退事件回调
             */
            onFallback: (handler) => {
                pushItem(fallbackHandlers, handler);
            },
            /**
             * 绑定入队列前事件
             * @param handler 入队列前的事件回调
             */
            onBeforePushQueue: (handler) => {
                pushItem(beforePushQueueHandlers, handler);
            },
            /**
             * 绑定入队列后事件
             * @param handler 入队列后的事件回调
             */
            onPushedQueue: (handler) => {
                pushItem(pushedQueueHandlers, handler);
            },
            /**
             * 重试事件
             * @param handler 重试事件回调
             */
            onRetry: (handler) => {
                pushItem(retryHandlers, handler);
            }
        }
    };
};

function useSQRequest_unified (handler, config = {}) {
    const { middleware = noop$1 } = config;
    const { c: methodCreateHandler, m: silentMiddleware, b: binders } = createSilentQueueMiddlewares(handler, config);
    const states = useRequest(methodCreateHandler, {
        ...config,
        middleware: (ctx, next) => {
            middleware(ctx, () => promiseResolve(undefinedValue));
            return silentMiddleware(ctx, next);
        }
    });
    return {
        ...states,
        ...binders
    };
}

const useAutoRequest$1 = (handler, config, onMounted$, onUnmounted$) => {
    let notifiable = trueValue;
    const { enableFocus = trueValue, enableVisibility = trueValue, enableNetwork = trueValue, pollingTime = 0, throttle = 1000 } = config, states = useRequest(handler, config), notify = () => {
        if (notifiable) {
            states.send();
            if (throttle > 0) {
                notifiable = falseValue;
                setTimeout(() => (notifiable = trueValue), throttle);
            }
        }
    };
    let offNetwork = noop$1, offFocus = noop$1, offVisiblity = noop$1, offPolling = noop$1;
    onMounted$(() => {
        if (!isSSR) {
            offNetwork = enableNetwork ? useAutoRequest$1.onNetwork(notify, config) : offNetwork;
            offFocus = enableFocus ? useAutoRequest$1.onFocus(notify, config) : offFocus;
            offVisiblity = enableVisibility ? useAutoRequest$1.onVisibility(notify, config) : offVisiblity;
            offPolling = pollingTime > 0 ? useAutoRequest$1.onPolling(notify, config) : offPolling;
        }
    });
    onUnmounted$(() => {
        offNetwork();
        offFocus();
        offVisiblity();
        offPolling();
    });
    return states;
};
const on = (type, handler) => {
    window.addEventListener(type, handler);
    return () => window.removeEventListener(type, handler);
};
useAutoRequest$1.onNetwork = notify => on('online', notify);
useAutoRequest$1.onFocus = notify => on('focus', notify);
useAutoRequest$1.onVisibility = notify => {
    const handle = () => document.visibilityState === 'visible' && notify();
    return on('visibilitychange', handle);
};
useAutoRequest$1.onPolling = (notify, config) => {
    const timer = setInterval(notify, config.pollingTime);
    return () => clearInterval(timer);
};

const hookPrefix$1 = 'useCaptcha';
const captchaAssert = createAssert(hookPrefix$1);
var useCaptcha_unified = (handler, config, $, upd$, _$, _exp$, useFlag$, useMemorizedCallback$) => {
    const { initialCountdown, middleware } = config;
    captchaAssert(initialCountdown === undefinedValue || initialCountdown > 0, 'initialCountdown must be greater than 0');
    const countdown = $(0, trueValue);
    const requestReturned = useRequest(handler, {
        ...config,
        immediate: falseValue,
        middleware: middleware ? (ctx, next) => middleware({ ...ctx, send }, next) : undefinedValue
    });
    const timer = useFlag$(undefinedValue);
    const send = useMemorizedCallback$((...args) => newInstance(PromiseCls, (resolve, reject) => {
        if (_$(countdown) <= 0) {
            requestReturned
                .send(...args)
                .then(result => {
                upd$(countdown, config.initialCountdown || 60);
                timer.current = setInterval(() => {
                    upd$(countdown, val => val - 1);
                    if (_$(countdown) <= 0) {
                        clearInterval(timer.current);
                    }
                }, 1000);
                resolve(result);
            })
                .catch(reason => reject(reason));
        }
        else {
            reject(new Error(buildErrorMsg(hookPrefix$1, 'the countdown is not over yet')));
        }
    }));
    return {
        ...requestReturned,
        send,
        countdown: _exp$(countdown)
    };
};

const getStoragedKey = (methodInstance, id) => `alova/form-${id || getMethodKey(methodInstance)}`;
const sharedStates = {};
const cloneFormData = (form) => {
    const shallowClone = (value) => isArray$1(value) ? [...value] : isPlainOrCustomObject(value) ? { ...value } : value;
    return walkObject(shallowClone(form), shallowClone);
};
const assert$3 = createAssert('useForm');
var useForm_unified = (handler, config, $, $$, _$, _exp$, upd$, watch$, onMounted$, useFlag$, useMemorizedCallback$) => {
    // 如果第一个参数传入的是id，则获取它的初始化数据并返回
    if (isNumber(handler) || isString(handler)) {
        const id = handler;
        const sharedState = sharedStates[id];
        assert$3(!!sharedState, `the form data of id \`${id}\` is not initial`);
        return sharedState.hookReturns;
    }
    const { id, initialForm, store, resetAfterSubmiting, immediate = falseValue, middleware } = config, form = $(cloneFormData(initialForm), trueValue), methodHandler = handler, restoreHandlers = [], 
    // 使用计算属性，避免每次执行此use hook都调用一遍methodHandler
    initialMethodInstance = $$(() => sloughConfig(methodHandler, [_$(form)]), []), isStoreObject = isObject(store), enableStore = isStoreObject ? store.enable : store, storageContext = getContext(_$(initialMethodInstance)).storage, storagedKey = useFlag$(getStoragedKey(_$(initialMethodInstance), id)), reseting = useFlag$(falseValue), serializerPerformer = useFlag$(createSerializerPerformer(isStoreObject ? store.serializers : undefinedValue)), 
    // 是否由当前hook发起创建的共享状态，发起创建的hook需要返回最新的状态，否则会因为在react中hook被调用，导致发起获得的hook中无法获得最新的状态
    isCreateShardState = useFlag$(false), 
    // 如果config中的id也有对应的共享状态，则也会返回它
    // 继续往下执行是为了兼容react的hook执行数不能变的问题，否则会抛出"Rendered fewer hooks than expected. This may be caused by an accidental early return statement."
    sharedState = id ? sharedStates[id] : undefinedValue;
    /**
     * 重置form数据
     */
    const reset = useMemorizedCallback$(() => {
        reseting.current = trueValue;
        upd$(form, cloneFormData(initialForm));
        enableStore && storageContext.remove(storagedKey.current);
    });
    /**
     * 更新form数据
     * @param newForm 新表单数据
     */
    const updateForm = useMemorizedCallback$((newForm) => {
        upd$(form, {
            ..._$(form),
            ...newForm
        });
    });
    let hookReturns = {
        form: _exp$(form),
        // 第一个参数固定为form数据
        ...useRequest((...args) => methodHandler(_$(form), ...args), {
            ...config,
            // 中间件函数，也支持subscriberMiddleware
            middleware: middleware
                ? (ctx, next) => middleware({
                    ...ctx,
                    delegatingActions: { updateForm, reset }
                }, next)
                : undefinedValue,
            // 1. 当需要持久化时，将在数据恢复后触发
            // 2. 当已有共享状态时，表示之前已有初始化（无论有无立即发起请求），后面的不再自动发起请求，这是为了兼容多表单立即发起请求时，重复发出请求的问题
            immediate: enableStore || sharedState ? falseValue : immediate
        }),
        // 持久化数据恢复事件绑定
        onRestore(handler) {
            pushItem(restoreHandlers, handler);
        },
        updateForm,
        reset
    };
    // 有id时，才保存到sharedStates中
    // 在react中，因为更新form后会产生新的form，因此需要每次调用重新保存
    if (id) {
        // 还没有共享状态则表示当前hook是创建的hook
        if (!sharedState) {
            isCreateShardState.current = trueValue;
        }
        // 只保存创建hook的共享状态
        if (isCreateShardState.current) {
            sharedStates[id] = {
                hookReturns,
                config
            };
        }
    }
    const { send, onSuccess } = hookReturns;
    onMounted$(() => {
        // 需要持久化时更新data
        if (enableStore && !sharedState) {
            // 获取存储并更新data
            // 需要在onMounted中调用，否则会导致在react中重复被调用
            const storagedForm = serializerPerformer.current.deserialize(storageContext.get(storagedKey.current));
            // 有草稿数据时，异步恢复数据，否则无法正常绑定onRetore事件
            if (storagedForm) {
                upd$(form, storagedForm);
                // 触发持久化数据恢复事件
                runArgsHandler(restoreHandlers);
                enableStore && immediate && send();
            }
        }
    });
    // 监听变化同步存储，如果是reset触发的则不需要再序列化
    watch$([form], () => {
        if (reseting.current || !enableStore) {
            reseting.current = falseValue;
            return;
        }
        storageContext.set(storagedKey.current, serializerPerformer.current.serialize(_$(form)));
    });
    // 如果在提交后需要清除数据，则调用reset
    onSuccess(() => {
        resetAfterSubmiting && reset();
    });
    // 有已保存的sharedState，则返回它
    // 如果是当前hook创建的共享状态，则返回最新的而非缓存的
    return sharedState && !isCreateShardState.current ? sharedState.hookReturns : hookReturns;
};

const hookPrefix = 'useRetriableRequest';
const assert$2 = createAssert(hookPrefix);
var useRetriableRequest_unified = (handler, config, useFlag$, useMemorizedCallback$) => {
    const { retry = 3, backoff = { delay: 1000 }, middleware = noop$1 } = config;
    const retryHandlers = [];
    const failHandlers = [];
    const retryTimes = useFlag$(0);
    const stopManuallyError = useFlag$(undefinedValue); // 停止错误对象，在手动触发停止时有值
    const methodInstanceLastest = useFlag$(undefinedValue);
    const sendArgsLatest = useFlag$(undefinedValue);
    const currentLoadingState = useFlag$(falseValue);
    const requesting = useFlag$(falseValue); // 是否正在请求
    const retryTimer = useFlag$(undefinedValue);
    const emitOnFail = (method, sendArgs, error) => {
        // 需要异步触发onFail，让onError和onComplete先触发
        setTimeoutFn(() => {
            runArgsHandler(failHandlers, createHookEvent(10, method, undefinedValue, undefinedValue, undefinedValue, retryTimes.current, undefinedValue, sendArgs, undefinedValue, undefinedValue, error));
            stopManuallyError.current = undefinedValue;
            retryTimes.current = 0; // 重置已重试次数
        });
    };
    /**
     * 停止重试，只在重试期间调用有效
     * 如果正在请求中，则触发中断请求，让请求错误来抛出错误，否则手动修改状态以及触发onFail
     * 停止后将立即触发onFail事件
     */
    const stop = useMemorizedCallback$(() => {
        assert$2(currentLoadingState.current, 'there are no requests being retried');
        stopManuallyError.current = new Error(buildErrorMsg(hookPrefix, 'stop retry manually'));
        if (requesting.current) {
            requestReturns.abort();
        }
        else {
            emitOnFail(methodInstanceLastest.current, sendArgsLatest.current, stopManuallyError.current);
            requestReturns.update({ error: stopManuallyError.current, loading: falseValue });
            currentLoadingState.current = falseValue;
            clearTimeoutFn(retryTimer.current); // 清除重试定时器
        }
    });
    const requestReturns = useRequest(handler, {
        ...config,
        middleware(ctx, next) {
            middleware({
                ...ctx,
                delegatingActions: {
                    stop
                }
            }, () => promiseResolve(undefinedValue));
            const { update, sendArgs, send, method, controlLoading } = ctx;
            const setLoading = (loading = falseValue) => {
                if (loading !== currentLoadingState.current) {
                    update({ loading });
                    currentLoadingState.current = loading;
                }
            };
            controlLoading();
            setLoading(trueValue);
            methodInstanceLastest.current = method;
            sendArgsLatest.current = sendArgs;
            requesting.current = trueValue;
            return promiseThen(next(), 
            // 请求成功时设置loading为false
            val => {
                retryTimes.current = 0; // 重置已重试次数
                requesting.current = falseValue;
                setLoading();
                return val;
            }, 
            // 请求失败时触发重试机制
            error => {
                // 没有手动触发停止，以及重试次数未到达最大时触发重试
                if (!stopManuallyError.current &&
                    (isNumber(retry) ? retryTimes.current < retry : retry(error, ...sendArgs))) {
                    // 计算重试延迟时间
                    const retryDelay = delayWithBackoff(backoff, ++retryTimes.current);
                    // 延迟对应时间重试
                    retryTimer.current = setTimeoutFn(() => {
                        // 如果手动停止了则不再触发重试
                        promiseCatch(send(...sendArgs), noop$1); // 捕获错误不再往外抛，否则重试时也会抛出错误
                        // 触发重试事件
                        runArgsHandler(retryHandlers, createHookEvent(9, method, undefinedValue, undefinedValue, undefinedValue, retryTimes.current, retryDelay, sendArgs));
                    }, retryDelay);
                }
                else {
                    setLoading();
                    error = stopManuallyError.current || error; // 如果stopManuallyError有值表示是通过stop函数触发停止的
                    emitOnFail(method, sendArgs, error);
                }
                requesting.current = falseValue;
                // 返回reject执行后续的错误流程
                return promiseReject(error);
            });
        }
    });
    /**
     * 重试事件绑定
     * 它们将在重试发起后触发
     * @param handler 重试事件回调
     */
    const onRetry = (handler) => {
        pushItem(retryHandlers, handler);
    };
    /**
     * 失败事件绑定
     * 它们将在不再重试时触发，例如到达最大重试次数时，重试回调返回false时，手动调用stop停止重试时
     * 而alova的onError事件是在每次请求报错时都将被触发
     *
     * 注意：如果没有重试次数时，onError、onComplete和onFail会被同时触发
     *
     * @param handler 失败事件回调
     */
    const onFail = (handler) => {
        pushItem(failHandlers, handler);
    };
    return {
        ...requestReturns,
        stop,
        onRetry,
        onFail
    };
};

/**
 * 构建完整的url
 * @param base baseURL
 * @param url 路径
 * @param params url参数
 * @returns 完整的url
 */
const buildCompletedURL = (baseURL, url, params) => {
    // baseURL如果以/结尾，则去掉/
    baseURL = baseURL.endsWith('/') ? baseURL.slice(0, -1) : baseURL;
    // 如果不是/或http协议开头的，则需要添加/
    url = url.match(/^(\/|https?:\/\/)/) ? url : `/${url}`;
    const completeURL = baseURL + url;
    // 将params对象转换为get字符串
    // 过滤掉值为undefined的
    const paramsStr = map(filterItem(objectKeys(params), key => params[key] !== undefinedValue), key => `${key}=${params[key]}`).join('&');
    // 将get参数拼接到url后面，注意url可能已存在参数
    return paramsStr
        ? +completeURL.includes('?')
            ? `${completeURL}&${paramsStr}`
            : `${completeURL}?${paramsStr}`
        : completeURL;
};

const assert$1 = createAssert('useSSE');
const MessageType = {
    Open: 'open',
    Error: 'error',
    Message: 'message'
};
var useSSE_unified = (handler, config = {}, $, $$, _$, _exp$, upd$, watch$, onMounted$, onUnmounted$, useFlag$, useMemorizedCallback$) => {
    const { initialData, withCredentials, interceptByGlobalResponded = trueValue, 
    /** abortLast = trueValue, */
    immediate = falseValue } = config;
    // ! 暂时不支持指定 abortLast
    const abortLast = trueValue;
    let eventSource = useFlag$(undefinedValue);
    let sendPromiseObject = useFlag$(undefinedValue);
    const data = $(initialData, trueValue);
    const readyState = $(2 /* SSEHookReadyState.CLOSED */, trueValue);
    let methodInstance = getHandlerMethod(handler);
    let responseUnified;
    // 储存自定义事件的 useCallback 对象，其中 key 为 eventName
    const customEventMap = new Map();
    const [onOpen, triggerOnOpen, offOpen] = useCallback();
    const [onMessage, triggerOnMessage, offMessage] = useCallback();
    const [onError, triggerOnError, offError] = useCallback();
    let responseSuccessHandler = __self, responseErrorHandler = __throw, responseCompleteHandler = noop$1;
    /**
     * 设置响应拦截器，在每次 send 之后都需要调用
     */
    const setResponseHandler = (methodInstance) => {
        const { responsed, responded } = getOptions(methodInstance);
        responseUnified = responded || responsed;
        if (isFn(responseUnified)) {
            responseSuccessHandler = responseUnified;
        }
        else if (responseUnified && isPlainOrCustomObject(responseUnified)) {
            const { onSuccess: successHandler, onError: errorHandler, onComplete: completeHandler } = responseUnified;
            responseSuccessHandler = isFn(successHandler) ? successHandler : responseSuccessHandler;
            responseErrorHandler = isFn(errorHandler) ? errorHandler : responseErrorHandler;
            responseCompleteHandler = isFn(completeHandler) ? completeHandler : responseCompleteHandler;
        }
    };
    /**
     * 处理响应任务，失败时不缓存数据
     * @param handlerReturns 拦截器返回后的数据
     * @returns 处理后的response
     */
    const handleResponseTask = async (handlerReturns) => {
        const { headers, name: methodInstanceName, transformData: transformDataFn = __self } = getConfig(methodInstance);
        const methodKey = getMethodInternalKey(methodInstance);
        const returnsData = await handlerReturns;
        const transformedData = await transformDataFn(returnsData, (headers || {}));
        upd$(data, transformedData);
        // 查找hitTarget
        const hitMethods = matchSnapshotMethod({
            filter: cachedMethod => (cachedMethod.hitSource || []).some(sourceMatcher => instanceOf(sourceMatcher, RegExp)
                ? sourceMatcher.test(methodInstanceName)
                : sourceMatcher === methodInstanceName || sourceMatcher === methodKey)
        });
        // 令符合条件(hitTarget定义)的method的缓存失效
        if (hitMethods.length > 0) {
            invalidateCache(hitMethods);
        }
        return transformedData;
    };
    /**
     * 创建 AlovaSSEHook 事件
     * 具体数据处理流程参考以下链接
     * @link https://alova.js.org/zh-CN/tutorial/combine-framework/response
     */
    const createSSEEvent = async (eventFrom, dataOrError) => {
        assert$1(!!eventSource.current, 'EventSource is not initialized');
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        const es = eventSource.current;
        const ev = (type, data, error) => {
            const event = createHookEvent(type, methodInstance, undefinedValue, undefinedValue, undefinedValue, undefinedValue, undefinedValue, undefinedValue, data, undefinedValue, error);
            event.eventSource = es;
            return event;
        };
        if (eventFrom === MessageType.Open) {
            return Promise.resolve(ev(11 /* AlovaHookEventType.SSEOpenEvent */));
        }
        const globalSuccess = interceptByGlobalResponded ? responseSuccessHandler : __self;
        const globalError = interceptByGlobalResponded ? responseErrorHandler : __throw;
        const globalFinally = interceptByGlobalResponded ? responseCompleteHandler : noop$1;
        const p = promiseFinally(promiseThen(dataOrError, data => handleResponseTask(globalSuccess(data, methodInstance)), error => handleResponseTask(globalError(error, methodInstance))), 
        // finally
        () => {
            globalFinally(methodInstance);
        });
        // 无论如何，函数返回的 Promise 对象一定都会 fulfilled
        return promiseThen(p, 
        // 得到处理好的数据（transform 之后的数据）
        data => ev(12 /* AlovaHookEventType.SSEMessageEvent */, data), 
        // 有错误
        error => ev(13 /* AlovaHookEventType.SSEErrorEvent */, undefinedValue, error));
    };
    /**
     * 根据事件选择需要的触发函数。如果事件无错误则触发传传入的回调函数
     * @param callback 无错误时触发的回调函数
     */
    const sendSSEEvent = (callback) => {
        return (event) => {
            if (event.error === undefinedValue) {
                return callback(event);
            }
            else {
                return triggerOnError(event);
            }
        };
    };
    // * MARK: EventSource 的事件处理
    const onCustomEvent = useMemorizedCallback$((eventName, handler) => {
        var _a;
        if (!customEventMap.has(eventName)) {
            const useCallbackObject = useCallback(callbacks => {
                var _a;
                if (callbacks.length === 0) {
                    (_a = eventSource.current) === null || _a === void 0 ? void 0 : _a.removeEventListener(eventName, useCallbackObject[1]);
                    customEventMap.delete(eventName);
                }
            });
            const trigger = useCallbackObject[1];
            customEventMap.set(eventName, useCallbackObject);
            (_a = eventSource.current) === null || _a === void 0 ? void 0 : _a.addEventListener(eventName, event => {
                promiseThen(createSSEEvent(eventName, Promise.resolve(event.data)), sendSSEEvent(trigger));
            });
        }
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        const [onEvent] = customEventMap.get(eventName);
        return onEvent(handler);
    });
    /**
     * 取消自定义事件在 useCallback 中的注册
     */
    const offCustomEvent = () => {
        customEventMap.forEach(([_1, _2, offTrigger]) => {
            offTrigger();
        });
    };
    const esOpen = () => {
        var _a;
        // resolve 使用 send() 时返回的 promise
        upd$(readyState, 1 /* SSEHookReadyState.OPEN */);
        promiseThen(createSSEEvent(MessageType.Open, Promise.resolve()), triggerOnOpen);
        // ! 一定要在调用 onOpen 之后 resolve
        (_a = sendPromiseObject.current) === null || _a === void 0 ? void 0 : _a.resolve();
    };
    const esError = (event) => {
        var _a;
        upd$(readyState, 2 /* SSEHookReadyState.CLOSED */);
        promiseThen(createSSEEvent(MessageType.Error, Promise.reject((_a = event === null || event === void 0 ? void 0 : event.message) !== null && _a !== void 0 ? _a : 'SSE Error')), sendSSEEvent(triggerOnMessage));
    };
    const esMessage = (event) => {
        promiseThen(createSSEEvent(MessageType.Message, Promise.resolve(event.data)), sendSSEEvent(triggerOnMessage));
    };
    /**
     * 关闭当前 eventSource 的注册
     */
    const close = useMemorizedCallback$(() => {
        const es = eventSource.current;
        if (!es) {
            return;
        }
        if (sendPromiseObject.current) {
            // 如果 close 时 promise 还在
            sendPromiseObject.current.resolve();
        }
        // * MARK: 解绑事件处理
        es.close();
        es.removeEventListener(MessageType.Open, esOpen);
        es.removeEventListener(MessageType.Error, esError);
        es.removeEventListener(MessageType.Message, esMessage);
        upd$(readyState, 2 /* SSEHookReadyState.CLOSED */);
        // eventSource 关闭后，取消注册所有自定义事件
        // 否则可能造成内存泄露
        customEventMap.forEach(([_, eventTrigger], eventName) => {
            es.removeEventListener(eventName, eventTrigger);
        });
    });
    /**
     * 发送请求并初始化 eventSource
     */
    const connect = useMemorizedCallback$((...sendArgs) => {
        let es = eventSource.current;
        let promiseObj = sendPromiseObject.current;
        if (es && abortLast) {
            // 当 abortLast === true，关闭之前的连接并重新建立
            close();
        }
        // 设置 send 函数使用的 promise 对象
        if (!promiseObj) {
            promiseObj = sendPromiseObject.current = usePromise();
            // open 后清除 promise 对象
            promiseObj.promise.finally(() => {
                promiseObj = undefinedValue;
            });
        }
        methodInstance = getHandlerMethod(handler, sendArgs);
        // 设置响应拦截器
        setResponseHandler(methodInstance);
        const { params } = getConfig(methodInstance);
        const { baseURL, url } = methodInstance;
        const fullURL = buildCompletedURL(baseURL, url, params);
        // 建立连接
        es = new EventSource(fullURL, { withCredentials });
        eventSource.current = es;
        upd$(readyState, 0 /* SSEHookReadyState.CONNECTING */);
        // * MARK: 注册处理事件
        // 注册处理事件 open error message
        es.addEventListener(MessageType.Open, esOpen);
        es.addEventListener(MessageType.Error, esError);
        es.addEventListener(MessageType.Message, esMessage);
        // 以及 自定义事件
        // 如果在 connect（send）之前就使用了 on 监听，则 customEventMap 里就已经有事件存在
        customEventMap.forEach(([_, eventTrigger], eventName) => {
            es === null || es === void 0 ? void 0 : es.addEventListener(eventName, event => {
                promiseThen(createSSEEvent(eventName, Promise.resolve(event.data)), sendSSEEvent(eventTrigger));
            });
        });
        return promiseObj.promise;
    });
    onUnmounted$(() => {
        close();
        // 上面使用 eventSource.removeEventListener 只是断开了 eventSource 和 trigger 的联系
        // 这里是取消 useCallback 对象中的事件注册
        offOpen();
        offMessage();
        offError();
        offCustomEvent();
    });
    // * MARK: 初始化动作
    onMounted$(() => {
        if (immediate) {
            connect();
        }
    });
    return {
        readyState: _exp$(readyState),
        data: _exp$(data),
        eventSource: _exp$(eventSource),
        send: connect,
        close,
        onMessage,
        onError,
        onOpen,
        on: onCustomEvent
    };
};

const actionsMap = {};
const isFrontMiddlewareContext = (context) => !!context.send;
const assert = createAssert('subscriber');
/**
 * 操作函数委托中间件
 * 使用此中间件后可通过accessAction调用委托的函数
 * 可以委托多个相同id
 * 以此来消除组件的层级限制
 * @param id 委托者id
 * @returns alova中间件函数
 */
const actionDelegationMiddleware$1 = (id, useFlag$) => {
    const delegated = useFlag$(falseValue);
    return (context, next) => {
        // 中间件会重复调用，已经订阅过了就无需再订阅了
        if (!delegated.current) {
            const { abort, update, delegatingActions = {} } = context;
            // 相同id的将以数组形式保存在一起
            const handlersItems = (actionsMap[id] = actionsMap[id] || []);
            handlersItems.push(isFrontMiddlewareContext(context)
                ? {
                    ...delegatingActions,
                    send: context.send,
                    abort,
                    update
                }
                : {
                    ...delegatingActions,
                    fetch: context.fetch,
                    abort,
                    update
                });
            delegated.current = trueValue;
        }
        return next();
    };
};
/**
 * 访问操作函数，如果匹配多个则会以此调用onMatch
 * @param id 委托者id，或正则表达式
 * @param onMatch 匹配的订阅者
 */
const accessAction = (id, onMatch) => {
    const matched = [];
    if (typeof id === 'symbol' || isString(id) || isNumber(id)) {
        assert(!!actionsMap[id], `no handler which id is \`${id.toString()}\` is matched`);
        pushItem(matched, ...actionsMap[id]);
    }
    else if (instanceOf(id, RegExp)) {
        forEach(filterItem(objectKeys(actionsMap), idItem => id.test(idItem)), idItem => {
            pushItem(matched, ...actionsMap[idItem]);
        });
    }
    forEach(matched, onMatch);
};

const defaultVisitorMeta = {
    authRole: null
}, defaultLoginMeta = {
    authRole: 'login'
}, defaultLogoutMeta = {
    authRole: 'logout'
}, defaultRefreshTokenMeta = {
    authRole: 'refreshToken'
}, checkMethodRole = ({ meta }, metaMatches) => {
    if (isPlainOrCustomObject(meta)) {
        for (const key in meta) {
            const matchedMetaItem = metaMatches[key];
            if (instanceOf(matchedMetaItem, RegExp) ? matchedMetaItem.test(meta[key]) : meta[key] === matchedMetaItem) {
                return trueValue;
            }
        }
    }
    return falseValue;
}, waitForTokenRefreshed = (method, waitingList) => newInstance(PromiseCls, resolve => {
    pushItem(waitingList, {
        method,
        resolve
    });
}), callHandlerIfMatchesMeta = (method, authorizationInterceptor, defaultMeta, response) => {
    if (checkMethodRole(method, (authorizationInterceptor === null || authorizationInterceptor === void 0 ? void 0 : authorizationInterceptor.metaMatches) || defaultMeta)) {
        const handler = isFn(authorizationInterceptor)
            ? authorizationInterceptor
            : isPlainOrCustomObject(authorizationInterceptor) &&
                isFn(authorizationInterceptor.handler)
                ? authorizationInterceptor.handler
                : noop$1;
        return handler(response, method);
    }
}, refreshTokenIfExpired = async (method, waitingList, updateRefreshStatus, handlerParams, refreshToken, tokenRefreshing) => {
    // 当handleParams数量大于2时，说明是从响应中调用此函数的，此时需要重新请求原接口
    const fromResponse = len(handlerParams) >= 2;
    let isExpired = refreshToken === null || refreshToken === void 0 ? void 0 : refreshToken.isExpired(...handlerParams);
    // 兼容处理同步函数和异步函数
    if (instanceOf(isExpired, PromiseCls)) {
        isExpired = await isExpired;
    }
    if (isExpired) {
        try {
            // 在响应中再次判断，防止请求多次刷新token，把在token刷新完成前发送的拦截并等待
            let intentToRefreshToken = trueValue;
            if (fromResponse && tokenRefreshing) {
                intentToRefreshToken = falseValue; // 在此等待的请求表示token刷新中，当它们通过后不再需要再次刷新token了
                await waitForTokenRefreshed(method, waitingList);
            }
            if (intentToRefreshToken) {
                updateRefreshStatus(trueValue);
                // 调用刷新token
                await (refreshToken === null || refreshToken === void 0 ? void 0 : refreshToken.handler(...handlerParams));
                updateRefreshStatus(falseValue);
                // 刷新token完成后，通知等待列表中的请求
                forEach(waitingList, ({ resolve }) => resolve());
            }
            if (fromResponse) {
                // 这里因为是重新请求原接口，与上一次请求叠加会导致重复调用transformData，因此需要将transformData置空去除一次调用
                const config = method.config, methodTransformData = config.transformData;
                config.transformData = undefinedValue;
                const resentData = await method;
                config.transformData = methodTransformData;
                return resentData;
            }
        }
        finally {
            updateRefreshStatus(falseValue);
            splice(waitingList, 0, len(waitingList)); // 清空waitingList
        }
    }
}, onResponded2Record = (onRespondedHandlers) => {
    let successHandler = undefinedValue, errorHandler = undefinedValue, onCompleteHandler = undefinedValue;
    if (isFn(onRespondedHandlers)) {
        successHandler = onRespondedHandlers;
    }
    else if (isPlainOrCustomObject(onRespondedHandlers)) {
        const { onSuccess, onError, onComplete } = onRespondedHandlers;
        successHandler = isFn(onSuccess) ? onSuccess : successHandler;
        errorHandler = isFn(onError) ? onError : errorHandler;
        onCompleteHandler = isFn(onComplete) ? onComplete : onCompleteHandler;
    }
    return {
        onSuccess: successHandler,
        onError: errorHandler,
        onComplete: onCompleteHandler
    };
};

/**
 * 创建客户端的token认证拦截器
 * @param options 配置参数
 * @returns token认证拦截器函数
 */
const createClientTokenAuthentication = ({ visitorMeta, login, logout, refreshToken, assignToken = noop$1 }) => {
    let tokenRefreshing = falseValue, waitingList = [];
    return {
        waitingList,
        onAuthRequired: onBeforeRequest => async (method) => {
            const isVisitorRole = checkMethodRole(method, visitorMeta || defaultVisitorMeta), isLoginRole = checkMethodRole(method, (login === null || login === void 0 ? void 0 : login.metaMatches) || defaultLoginMeta);
            // 被忽略的、登录、刷新token的请求不进行token认证
            if (!isVisitorRole &&
                !isLoginRole &&
                !checkMethodRole(method, (refreshToken === null || refreshToken === void 0 ? void 0 : refreshToken.metaMatches) || defaultRefreshTokenMeta)) {
                // 如果正在刷新token，则等待刷新完成后再发请求
                if (tokenRefreshing) {
                    await waitForTokenRefreshed(method, waitingList);
                }
                await refreshTokenIfExpired(method, waitingList, refreshing => (tokenRefreshing = refreshing), [method], refreshToken);
            }
            // 非访客和登录角色的请求会进入赋值token函数
            if (!isVisitorRole && !isLoginRole) {
                await assignToken(method);
            }
            return onBeforeRequest === null || onBeforeRequest === void 0 ? void 0 : onBeforeRequest(method);
        },
        onResponseRefreshToken: onRespondedHandlers => {
            const respondedRecord = onResponded2Record(onRespondedHandlers);
            return {
                ...respondedRecord,
                onSuccess: async (response, method) => {
                    await callHandlerIfMatchesMeta(method, login, defaultLoginMeta, response);
                    await callHandlerIfMatchesMeta(method, logout, defaultLogoutMeta, response);
                    return (respondedRecord.onSuccess || __self)(response, method);
                }
            };
        }
    };
};
/**
 * 创建服务端的token认证拦截器
 * @param options 配置参数
 * @returns token认证拦截器函数
 */
const createServerTokenAuthentication = ({ visitorMeta, login, logout, refreshTokenOnSuccess, refreshTokenOnError, assignToken = noop$1 }) => {
    let tokenRefreshing = falseValue, waitingList = [];
    return {
        waitingList,
        onAuthRequired: onBeforeRequest => async (method) => {
            const isVisitorRole = checkMethodRole(method, visitorMeta || defaultVisitorMeta), isLoginRole = checkMethodRole(method, (login === null || login === void 0 ? void 0 : login.metaMatches) || defaultLoginMeta);
            // 被忽略的、登录、刷新token的请求不进行token认证
            if (!isVisitorRole &&
                !isLoginRole &&
                !checkMethodRole(method, (refreshTokenOnSuccess === null || refreshTokenOnSuccess === void 0 ? void 0 : refreshTokenOnSuccess.metaMatches) || defaultRefreshTokenMeta) &&
                !checkMethodRole(method, (refreshTokenOnError === null || refreshTokenOnError === void 0 ? void 0 : refreshTokenOnError.metaMatches) || defaultRefreshTokenMeta)) {
                // 如果正在刷新token，则等待刷新完成后再发请求
                if (tokenRefreshing) {
                    await waitForTokenRefreshed(method, waitingList);
                }
            }
            if (!isVisitorRole && !isLoginRole) {
                await assignToken(method);
            }
            return onBeforeRequest === null || onBeforeRequest === void 0 ? void 0 : onBeforeRequest(method);
        },
        onResponseRefreshToken: onRespondedHandlers => {
            const respondedRecord = onResponded2Record(onRespondedHandlers);
            return {
                ...respondedRecord,
                onSuccess: async (response, method) => {
                    if (!checkMethodRole(method, visitorMeta || defaultVisitorMeta) &&
                        !checkMethodRole(method, (login === null || login === void 0 ? void 0 : login.metaMatches) || defaultLoginMeta) &&
                        !checkMethodRole(method, (refreshTokenOnSuccess === null || refreshTokenOnSuccess === void 0 ? void 0 : refreshTokenOnSuccess.metaMatches) || defaultRefreshTokenMeta)) {
                        const dataResent = await refreshTokenIfExpired(method, waitingList, refreshing => (tokenRefreshing = refreshing), [response, method], refreshTokenOnSuccess, tokenRefreshing);
                        if (dataResent) {
                            return dataResent;
                        }
                    }
                    await callHandlerIfMatchesMeta(method, login, defaultLoginMeta, response);
                    await callHandlerIfMatchesMeta(method, logout, defaultLogoutMeta, response);
                    return (respondedRecord.onSuccess || __self)(response, method);
                },
                onError: async (error, method) => {
                    if (!checkMethodRole(method, visitorMeta || defaultVisitorMeta) &&
                        !checkMethodRole(method, (login === null || login === void 0 ? void 0 : login.metaMatches) || defaultLoginMeta) &&
                        !checkMethodRole(method, (refreshTokenOnError === null || refreshTokenOnError === void 0 ? void 0 : refreshTokenOnError.metaMatches) || defaultRefreshTokenMeta)) {
                        const dataResent = await refreshTokenIfExpired(method, waitingList, refreshing => (tokenRefreshing = refreshing), [error, method], refreshTokenOnError, tokenRefreshing);
                        if (dataResent) {
                            return dataResent;
                        }
                    }
                    return (respondedRecord.onError || noop$1)(error, method);
                }
            };
        }
    };
};

/**
 * 断言serialHandlers
 * @param hookName hook name
 * @param serialHandlers 串行请求method获取函数
 */
const assertSerialHandlers = (hookName, serialHandlers) => createAssert(hookName)(isArray$1(serialHandlers) && len(serialHandlers) > 0, 'please use an array to represent serial requests');
/**
 * 创建串行请求中间件
 * @param serialHandlers 串行请求method获取函数
 * @param hookMiddleware use hook的中间件
 * @returns 串行请求中间件
 */
const serialMiddleware = (serialHandlers, hookMiddleware) => {
    // 第一个handler在外部传递给了use hook，不需要再次请求
    shift(serialHandlers);
    return ((ctx, next) => {
        hookMiddleware === null || hookMiddleware === void 0 ? void 0 : hookMiddleware(ctx, () => promiseResolve(undefinedValue));
        ctx.controlLoading();
        ctx.update({ loading: trueValue });
        const methods = [];
        let serialPromise = next();
        for (const i in serialHandlers) {
            serialPromise = promiseThen(serialPromise, value => {
                const methodItem = serialHandlers[i](value, ...ctx.sendArgs);
                pushItem(methods, methodItem);
                return methodItem.send();
            });
        }
        // 装饰错误回调函数，将event.method设置为出错的实例
        ctx.decorateError((handler, event) => {
            event.method = methods[len(methods) - 1];
            handler(event);
        });
        return serialPromise.finally(() => {
            ctx.update({ loading: falseValue });
        });
    });
};

/**
 * 串行请求hook，每个serialHandlers中将接收到上一个请求的结果
 * 适用场景：串行请求一组接口
 * @param serialHandlers 串行请求回调数组
 * @param config 配置参数
 * @return useSerialRequest相关数据和操作函数
 */
var useSerialRequest = (serialHandlers, config = {}) => {
    assertSerialHandlers('useSerialRequest', serialHandlers);
    return useRequest(serialHandlers[0], {
        ...config,
        middleware: serialMiddleware(serialHandlers, config.middleware)
    });
};

/**
 * 串行请求hook，每个serialHandlers中将接收到上一个请求的结果
 * 适用场景：监听状态变化后，串行请求一组接口
 * @param serialHandlers 串行请求回调数组
 * @param config 配置参数
 * @return useSerialRequest相关数据和操作函数
 */
var useSerialWatcher = (serialHandlers, watchingStates, config = {}) => {
    assertSerialHandlers('useSerialWatcher', serialHandlers);
    return useWatcher(serialHandlers[0], watchingStates, {
        ...config,
        middleware: serialMiddleware(serialHandlers, config.middleware)
    });
};

/**
 * 反序列化silentMethod实例，根据序列化器的名称进行反序列化
 * @param methodInstance 请求方法实例
 * @returns 请求方法实例
 */
var convertPayload2SilentMethod = (payload) => {
    const { id, behavior, entity, retryError, maxRetryTimes, backoff, fallbackHandlers, resolveHandler, rejectHandler, handlerArgs, targetRefMethod, force } = payload;
    // method类实例化
    const deserializeMethod = (methodPayload) => {
        const { type, url, config, data } = methodPayload;
        return newInstance(Method, type, dependentAlovaInstance, url, config, data);
    };
    const silentMethodInstance = newInstance(SilentMethod, deserializeMethod(entity), behavior, id, force, retryError, maxRetryTimes, backoff, fallbackHandlers, resolveHandler, rejectHandler, handlerArgs);
    silentMethodInstance.cache = trueValue;
    // targetRefMethod反序列化
    if (targetRefMethod) {
        silentMethodInstance.targetRefMethod = deserializeMethod(targetRefMethod);
    }
    // 将额外的内容放到silentMethod实例上
    forEach(objectKeys(payload), key => {
        if (!includes([
            'id',
            'behavior',
            'entity',
            'retryError',
            'maxRetryTimes',
            'backoff',
            'fallbackHandlers',
            'resolveHandler',
            'rejectHandler',
            'handlerArgs',
            'targetRefMethod',
            'force'
        ], key)) {
            silentMethodInstance[key] = payload[key];
        }
    });
    return silentMethodInstance;
};

/**
 * 从storage中载入静默队列数据
 * @returns 所有队列数据
 */
var loadSilentQueueMapFromStorage = () => {
    const silentMethodIdQueueMap = (storageGetItem(silentMethodIdQueueMapStorageKey) ||
        {});
    const silentQueueMap = {};
    forEach(objectKeys(silentMethodIdQueueMap), queueName => {
        const currentQueue = (silentQueueMap[queueName] = silentQueueMap[queueName] || []);
        forEach(silentMethodIdQueueMap[queueName], silentMethodId => {
            const serializedSilentMethodPayload = storageGetItem(silentMethodStorageKeyPrefix + silentMethodId);
            serializedSilentMethodPayload &&
                pushItem(currentQueue, convertPayload2SilentMethod(serializedSilentMethodPayload));
        });
    });
    return silentQueueMap;
};

const offEventCallback = (offHandler, handlers) => () => {
    const index = handlers.indexOf(offHandler);
    index >= 0 && splice(handlers, index, 1);
};
/**
 * 绑定silentSubmit启动事件
 * @param {SilentSubmitBootHandler} handler 事件回调函数
 * @returns 解绑函数
 */
const onSilentSubmitBoot = (handler) => {
    pushItem(bootHandlers, handler);
    return offEventCallback(handler, bootHandlers);
};
/**
 * 绑定silentSubmit成功事件
 * @param {SilentSubmitSuccessHandler} handler 事件回调函数
 * @returns 解绑函数
 */
const onSilentSubmitSuccess = (handler) => {
    pushItem(successHandlers, handler);
    return offEventCallback(handler, successHandlers);
};
/**
 * 绑定silentSubmit错误事件
 * 每次请求错误，触发错误回调
 * @param {SilentSubmitErrorHandler} handler 事件回调函数
 * @returns 解绑函数
 */
const onSilentSubmitError = (handler) => {
    pushItem(errorHandlers, handler);
    return offEventCallback(handler, errorHandlers);
};
/**
 * 绑定silentSubmit失败事件
 * 失败事件将在最大请求次数到达，或不匹配错误信息时触发
 * @param {SilentSubmitFailHandler} handler 事件回调函数
 * @returns 解绑函数
 */
const onSilentSubmitFail = (handler) => {
    pushItem(failHandlers, handler);
    return offEventCallback(handler, failHandlers);
};
/**
 * 绑定silentSubmit发起请求前事件
 * @param {BeforeSilentSubmitHandler} handler 事件回调函数
 * @returns 解绑函数
 */
const onBeforeSilentSubmit = (handler) => {
    pushItem(beforeHandlers, handler);
    return offEventCallback(handler, beforeHandlers);
};
/**
 * 启动静默提交，它将载入缓存中的静默方法，并开始静默提交
 * 如果未传入延迟时间，则立即同步启动
 * @param {SilentFactoryBootOptions} options 延迟毫秒数
 */
const bootSilentFactory = (options) => {
    var _a;
    if (silentFactoryStatus === 0) {
        const { alova } = options;
        setDependentAlova(alova);
        setCustomSerializers(options.serializers);
        setQueueRequestWaitSetting(options.requestWait);
        setTimeoutFn(() => {
            // 延时加载，让页面的queue放在最前面
            merge2SilentQueueMap(loadSilentQueueMapFromStorage());
            // 循环启动队列静默提交
            // 多条队列是并行执行的
            forEach(objectKeys(silentQueueMap), queueName => {
                bootSilentQueue(silentQueueMap[queueName], queueName);
            });
            setSilentFactoryStatus(1); // 设置状态为已启动
            runArgsHandler(bootHandlers);
        }, (_a = options.delay) !== null && _a !== void 0 ? _a : 500);
    }
};

/**
 * 以兼容虚拟数据的方式判断两个值是否相等
 * @param prevValue 前项值
 * @param nextValue 后项值
 * @returns 是否相等
 */
var equals = (prevValue, nextValue) => {
    // 如果相等则直接返回
    if (prevValue === nextValue) {
        return trueValue;
    }
    return stringifyVData(prevValue) === stringifyVData(nextValue);
};

/**
 * 按method名称或正则表达式筛选满足条件的所有silentMethod实例
 * @param methodNameMatcher method名称匹配器
 * @param queueName 查找队列名，默认为default队列
 * @param filterActive 是否过滤掉激活状态的实例
 * @returns silentMethod实例数组
 */
const filterSilentMethods = (methodNameMatcher, queueName = DEFAUT_QUEUE_NAME, filterActive = falseValue) => {
    const matchSilentMethods = (targetQueue = []) => targetQueue.filter(silentMethodItem => {
        if (methodNameMatcher === undefinedValue) {
            return trueValue;
        }
        const name = getConfig(silentMethodItem.entity).name || '';
        const retain = instanceOf(methodNameMatcher, RegExp)
            ? regexpTest(methodNameMatcher, name)
            : name === methodNameMatcher;
        return retain && (filterActive ? silentMethodItem.active : trueValue);
    });
    return [
        ...matchSilentMethods(silentQueueMap[queueName]),
        // 如果当前未启动silentFactory，则还需要去持久化存储中匹配silentMethods
        ...(silentFactoryStatus === 0 ? matchSilentMethods(loadSilentQueueMapFromStorage()[queueName]) : [])
    ];
};
/**
 * 按method名称或正则表达式查找第一个满足条件的silentMethod实例
 * @param methodNameMatcher method名称匹配器
 * @param queueName 查找队列名，默认为default队列
 * @param filterActive 是否过滤掉激活状态的实例
 * @returns silentMethod实例，未找到时为undefined
 */
const getSilentMethod = (methodNameMatcher, queueName = DEFAUT_QUEUE_NAME, filterActive = falseValue) => filterSilentMethods(methodNameMatcher, queueName, filterActive)[0];

/**
 * 判断目标数据是否为虚拟数据
 * @param target 目标数据
 * @returns 是否为虚拟数据
 */
var isVData = (target) => !!stringifyVData(target, falseValue) || regexpTest(regVDataId, target);

function noop() { }

/**
 * 更新对应method的状态
 * 与updateState不同的是，除了立即更新状态外，它还会在silent模式下响应后再次更新一次，目的是将虚拟数据替换为实际数据
 * @param method 请求方法对象
 * @param handleUpdate 更新回调
 */
const updateStateEffect = (matcher, handleUpdate, options = {}) => {
    const onMatch = options.onMatch;
    options.onMatch = method => {
        // 将目标method实例保存到当前的silentMethod实例
        if (currentSilentMethod) {
            currentSilentMethod.setUpdateState(method, isFn(updateState) ? undefinedValue : objectKeys(updateState));
            currentSilentMethod.save();
        }
        (onMatch || noop)(method);
    };
    return updateState(matcher, handleUpdate, options);
};

const usePagination = (handler, config = {}) =>
  usePagination_unified(
    handler,
    config,
    $,
    $$,
    upd$,
    _$,
    _exp$,
    _expBatch$,
    watch$,
    useFlag$,
    useRequestRefState$,
    useMemorizedCallback$
  );

// 已通过 hooks/silent/useSQRequest 导入测试
/* c8 ignore start */
const useSQRequest = (handler, config = {}) => useSQRequest_unified(handler, config);

// 导出actionDelegation中间件
const actionDelegationMiddleware = id => actionDelegationMiddleware$1(id, useFlag$);

// 导出useCaptcha
const useCaptcha = (handler, config = {}) =>
  useCaptcha_unified(handler, config, $, upd$, _$, _exp$, useFlag$, useMemorizedCallback$);

// 导出useForm
const useForm = (handler, config = {}) =>
  useForm_unified(handler, config, $, $$, _$, _exp$, upd$, watch$, onMounted$, useFlag$, useMemorizedCallback$);

// 导出useRetriableRequest
const useRetriableRequest = (handler, config = {}) =>
  useRetriableRequest_unified(handler, config, useFlag$, useMemorizedCallback$);

// 导出useAutoRequest
const useAutoRequest = (handler, config = {}) =>
  useAutoRequest$1(handler, config, onMounted$, onUnmounted$);
forEach(objectKeys(useAutoRequest$1), key => {
  defineProperty(
    useAutoRequest,
    key,
    {
      get: () => useAutoRequest$1[key],
      set: value => (useAutoRequest$1[key] = value)
    },
    trueValue
  );
});

// 导出useSSE
const useSSE = (handler, config = {}) =>
  useSSE_unified(
    handler,
    config,
    $,
    $$,
    _$,
    _exp$,
    upd$,
    watch$,
    onMounted$,
    onUnmounted$,
    useFlag$,
    useMemorizedCallback$
  );

export { accessAction, actionDelegationMiddleware, bootSilentFactory, createClientTokenAuthentication, createServerTokenAuthentication, dehydrateVData, equals, filterSilentMethods, getSilentMethod, isVData, onBeforeSilentSubmit, onSilentSubmitBoot, onSilentSubmitError, onSilentSubmitFail, onSilentSubmitSuccess, silentQueueMap, stringifyVData, updateStateEffect, useAutoRequest, useCaptcha, useForm, usePagination, useRetriableRequest, useSQRequest, useSSE, useSerialRequest, useSerialWatcher };
